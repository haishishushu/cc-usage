# CC Usage 功能审查与待办

- [x] **2026-09-16 / 网关允许 HTTP**：按鼠鼠要求，前后端均允许普通域名、内网 IP 和本机地址使用 HTTP 或 HTTPS；移除仅回环地址允许 HTTP 的限制及旧提示。协议 / 地址校验回归通过，未向用户网关发送测试凭证。

审查日期：2026-09-16。依据：当前工作区代码、[需求文档](prd/Pencil-UI设计需求.md)、前后端调用链及本轮会话已有运行验证。

**结论：主面板已有真实查询与连接管理，但仍有只读占位设置、外部接口联调和桌面验收缺口。本轮重新核对总览、连接/来源选择、日期筛选、常规/外观/数据设置及灵动岛菜单，修复原生控件与错误状态文案。历史已勾选项不等于整项功能已通过实机验收。**

进度以每条记录及其验证证据为准，不再用历史勾选比例表示整个项目完成率。

## 按 pencil-new.pen 执行（2026-09-16，本轮最新要求）

**执行规则：缺失功能可直接补齐；替换已有 UI 必须先由鼠鼠决定。** 保留现有实现和设计文件，不为消除差异擅自覆盖任何界面。单智能体执行，不提交 Git。前序已排除的 Gemini/Grok、独立分析页和额外采集来源继续按原型“待接入 / 待确认”处理。

### 原型功能索引

浅深色成对画板共用同一功能实现；本表覆盖 00–19 类，不将示例数值和未知接口能力当作必做功能。

| 原型 | 对应代码 | 当前结论 / 后续条目 |
|---|---|---|
| 00 设计变量、基础组件 | `index.css`、`components/ui` | 已实现；不覆盖已采用的变量和字体 |
| 01/02 Auth 收缩及缺失态 | `UsageIsland.tsx`、`authQuotaRows.ts` | 已实现；单独保留未知 5h，避免把 7d 当 5h；紧凑布局差异见 PEN-UI-02 |
| 03 Auth 展开、会话 | `UsageIsland.tsx`、`SessionList.tsx` | 已实现实时会话与时间后 Token；已补独立今日统计行，见 PEN-UI-01 |
| 04/05 API 收缩与展开 | `IslandWindow.tsx`、`useQuota.ts` | 已实现实际费用优先和本机估算回退；本轮补 PEN-F01/F02 |
| 06 会话变体 | `useLiveUsage.ts`、`SessionList.tsx` | 已实现 3 条上限、其余数量提示、标题省略、来源中断状态；不虚构运行会话 |
| 07 Token 动效 | `liveTokenCounter.ts`、`TokenDelta.tsx` | 已实现去重、切换清空、恢复快照、减少动态效果；已有回归 |
| 08 总览、趋势、日志 | `OverviewView.tsx`、`useUsage.ts` | 已实现连接与灵动岛独立选择、周期查询、趋势、七列日志；源日志未知状态保留“已记录”，不补造成功或 HTTP 200 |
| 09 API 余额 | `BalanceCard.tsx`、`quota.rs` | 已实现余额五态、权限/不支持区别、提醒阈值；真实账号覆盖见 QA-02 |
| 10 自定义时间 | `DateRangePicker.tsx` | 已实现端点编辑、翻月、快捷周期、校验、确定与取消；不改变额度窗口 |
| 11 日志与分页 | `RequestLogTable.tsx`、`Pagination.tsx` | 已实现空/失败/加载/不支持、页码省略及跳转、未知总数降级、窄窗滚动 |
| 12 灵动岛设置与来源 | `SettingsView.tsx`、`settings.rs` | 已实现独立配置、唯一来源只读、真实能力预览；旧稿“打开主面板”入口与已确认“切换连接”冲突时保留最新用户要求 |
| 13 常规、外观、数据 | `SettingsView.tsx`、`db.rs` | 已实现刷新、贴边、免打扰、透明度、大小、主题、导入导出和清理；固定策略不伪装成可切换项 |
| 14 多平台扩展 | `platforms.ts` | Claude/Codex 已实现；Gemini/Grok 为原型明确的“待接入”，仍属 OPT-06 暂缓 |
| 15 状态与按需加载 | `useQuota.ts`、`IslandWindow.tsx`、`lib.rs` | 已实现失败保留旧额度、到期刷新和窗口释放；本轮补 PEN-F03；完整视觉验收仍见 QA-07 |
| 16 应用图标 | `AppIcon.tsx`、`backend/icons` | 已实现；托盘表现与画板 18 的差异见 PEN-UI-06 |
| 17 拖动与停靠 | `dock.rs`、`DockedIsland.tsx` | 已实现四边、探出、双击、居中提示与恢复；按用户要求仅验收单屏 |
| 18 托盘与菜单 | `context_menu.rs`、`ContextMenuWindow.tsx` | 操作已实现；已补完整悬停摘要和原型角标，见 PEN-UI-05/06 |
| 19 连接管理 | `ConnectionDialogs.tsx`、`connections.rs` | 已实现添加、获取、CLI 授权、更换、移除与失败保留；已有隔离流程证据 |

### 可直接执行的功能缺口

- [x] **PEN-F01 / 画板 04/05 / 非零微小费用**：原实现 `.toFixed(2)` 会把 `$0.003` 显示成 `$0.00`。已在 `islandPresentation.ts` 保留小数精度，更小非零值显示 `<$0.0001`；未知、不完整或非法金额不伪造为零。4 项针对性回归和前端构建通过。
- [x] **PEN-F02 / 画板 04/05/12 / 估算费用标识**：原实现未传入已有的 `costEstimated` 标志。已接通该标志，使用现有“今日费用（估算）”展示分支；实际账单金额（包括真实 0）优先于估算，不改布局。
- [x] **PEN-F03 / 画板 15 / 真实连接状态**：原灵动岛仅区分 expired，其余连接一律显示已连接。已补齐 invalid → 凭证无效、offline → 离线；删除的所选连接保持不可用，不自动换号。沿用已有状态点与文字，不替换徽标样式。

### 已有 UI 差异：6 项已批准并实现

鼠鼠已明确批准“6 项都按原型修改”。以下 6 项已落到项目代码；实现完成不等于所有实机验收完成。

| 编号 | 当前实现 | 原型目标 | 影响 |
|---|---|---|---|
| PEN-UI-01 | Auth 展开态没有独立今日 Token 行 | 会话列表下方增加“本地今日 Token” | 改变展开高度；接口数据已有 |
| PEN-UI-02 | 额度行使用弹性轨道、较宽倒计时列 | 恢复短轨道、紧凑数字与倒计时列 | 改变已用额度行的空间比例 |
| PEN-UI-03 | 连接状态为点＋文字 | 使用浅色背景状态徽标 | 替换现有状态外观 |
| PEN-UI-04 | 来源说明按分隔符多行显示 | 紧凑页脚 | 改变展开高度与常驻说明数量，须保留查看完整内容的方法 |
| PEN-UI-05 | 托盘悬停只有最高百分比和等级 | 平台、连接、各额度、更新时间 | 扩充现有托盘摘要；自绘浮层外观需同时决定 |
| PEN-UI-06 | 托盘图标内部水位填充 | 原型水位角标及描边 | 替换现有托盘图标；额度计算保持不变 |

#### 本轮实现与证据

- [x] PEN-UI-01：Auth 展开态显示独立“本地今日 Token”，沿用真实本机累计值，不与本轮会话 Token 混淆。
- [x] PEN-UI-02：额度行采用 26 / 84 / 34 / 56px 固定列，收缩和展开均保留倒计时；浅深色检查无溢出，收缩态不碰撞右侧 Token。
- [x] PEN-UI-03：连接状态采用浅色背景徽标，保留已连接、过期、离线、无效的真实状态映射。
- [x] PEN-UI-04：来源说明压为一行；自绘提示支持悬停、键盘聚焦查看全文和 Escape 关闭。
- [x] PEN-UI-05：新增按需创建的自绘托盘摘要窗口，读取当前连接缓存，展示平台 / 连接 / 所有额度窗口 / 最近更新时间。缓存命中不重置更新时间，切换连接不显示上一个账号的数据；不支持、无权限、失效、限流和失败保持独立说明。窗口禁止获取焦点、鼠标穿透，离开或点击托盘销毁；350ms 延迟用序号取消过期创建。
- [x] PEN-UI-06：复用应用图标，添加原型绿 / 橙 / 红 / 灰右下角角标和白描边；最高额度阈值规则保持不变。
- 验证：前端 TypeScript / Vite 构建通过；Rust `cargo test --lib tray_` 5 项通过，覆盖最高额度、未知与真实零、连接缓存选择、工作区限位、角标颜色与描边。
- 浏览器证据：`output/design-sync/approved-six.cjs` 通过，截图 `approved-six-light.png` / `approved-six-dark.png`；来源说明键盘操作通过。
- 隔离桌面 IPC 证据：`output/design-sync/tray-summary-desktop.cjs` 通过。合成网关返回 20% / 40% / 未知三个窗口；缓存时间稳定、切换隔离、不支持状态均通过。双击展开内容底部 234.65px < 窗口 250px，右边 414px < 窗口 428px。
- [ ] 待实机确认：真实任务栏连续悬停 / 移出、不抢焦点以及深浅任务栏角标辨识度。现有自动化已验证数据、布局和像素逻辑，未把它冒充物理鼠标悬停验收。
- 交付：`pnpm tauri build` 成功，已生成含本轮 6 项修改的 `backend/target/release/bundle/nsis/CC Usage_0.1.0_x64-setup.exe`；隔离测试应用及合成网关已关闭。

### 非缺失功能，仍需保留的验收 / 范围项

QA-02 缺少其他真实账号；QA-05 缺少干净 Windows 环境；QA-06 长期低占用尚未证实；QA-07 完整视觉状态尚未全部验收。它们不能通过补代码或改为勾选来冒充完成。OPT-03 的其他语言、OPT-05/06/07 的扩展需求仍未纳入本轮。

## 清单使用规则

- `[x]`：该条描述的有限功能已有实现，静态调用链可确认；不代表相邻功能、所有边界情况或真实账号联调也完成。
- `[ ] 未实现`：未找到相应行为；`[ ] 部分实现`：已有部分代码但流程未闭合；`[ ] 缺陷`：现有逻辑与预期不符；`[ ] 待验证`：缺少实际运行证据；`[ ] 待确认`：原需求尚未确定，不擅自纳入首期必做。
- P0：错误数据、漏记风险或核心流程不可用；P1：首期核心功能缺口；P2：体验、资源占用、交付与文档；P3：可选扩展。
- 后续完成一项后，在**同一条**改成 `[x]`，补完成日期、验证方法和结果；部分完成继续留空，不另建一份互相矛盾的进度表。
- 本文为持续同步的实现清单；完成项记录日期与验证证据，未勾选项仍按当前代码状态保留。2026-09-15 本轮新增四类 Claude/Codex Auth/API 适配，但没有使用真实 Admin API Key 发起组织用量联调，也没有执行删除、导入导出或逐画板视觉验收。

## 六项验收跟进（2026-09-16）

用户确认单屏，真实账号为本机 Claude API Key 与 Codex Auth；执行报告和缺口见 [验收记录](docs/validation/2026-09-16.md)，工具见 [验收脚本说明](scripts/acceptance/README.md)。

## Pencil UI 风格同步（2026-09-16）

- [x] 保留现有功能，更新 `prd/pencil-new.pen` 的浅深色菜单、展开态、会话行和常规设置；以原型的颜色、尺寸、细边框、字体和间距统一实现。
- [x] 普通菜单开关取消整行蓝底，连接选项增加平台图标；160px 设置控件、余额提醒设置行、1064px 连接表、字体离线分发已落地。
- [x] 菜单键盘返回、连接切换与未配置禁用、长操作提示和窄窗口布局验证通过；前端构建及 18 项回归通过。证据见 [设计验收](design-qa.md)。

## 会话行实时 Token（2026-09-16）

- [x] 每行按“会话标题 → 本轮执行时间 → 该会话 Token”显示，长标题省略，时间与 Token 不挤压换行；顶部保留合计。
- [x] Codex 后端提供按会话、本轮起点统计的累计快照；重开恢复、心跳不重复叠加、新一轮归零。前端有快照时不再叠加同一事件增量。无可信起点的来源沿用真实采集增量，未知显示 `— Token`。
- 验证：多会话独立数值、合计、恢复和新一轮归零回归通过；TypeScript 与前端生产构建通过。后续六项验收阶段已重新打包，包含本次会话 Token 修改。

## 当前执行记录（2026-09-16，单智能体）

鼠鼠已选择本轮完成 Claude/Codex 与现有设置；Gemini/Grok、独立分析页和额外采集器（OPT-05/06/07）暂不纳入。本轮按原有编号推进，不删除未验收项。

本轮已完成 CONN-06、OPT-01/02/04、UI-02/07、VIEW-14、STAT-15；OPT-03 已完成刷新间隔，语言继续仅简体中文。另修复停靠时设置锁与窗口事件互等导致的卡死，拆分按需加载。

桌面证据：[自绘菜单](output/playwright/todo-desktop-menu.png)、[设置页](output/playwright/todo-desktop-settings.png)、[右侧展开及连接菜单](output/playwright/todo-island-right.png)。测试临时调试端口及前端夹具已移除；未修改用户凭证和历史数据。

验证：前端 18 项回归通过；真实桌面设置同步、CLI 引导打开/取消、主面板释放/重建、三档大小和四边展开通过。授权成功/失败及余额提醒使用隔离模拟接口验证。最终 Rust 回归 50 项通过、2 项真实环境测试默认跳过；TypeScript、前端生产构建及最终 NSIS 安装包构建通过。`git diff --check` 无空白错误。构建仍有 Tauri window 模块混合导入提示和 MSVC 链接器输出提示，不影响成功产出。

## 本轮主面板复核与执行顺序（2026-09-16）

依据：`OverviewView.tsx`、`SettingsView.tsx`、`UsageCard.tsx`、`DateRangePicker.tsx`、`IslandConnectionSwitcher.tsx`、查询 hooks 与后端命令；视觉以需求文档 A.2—A.4 为基线。本轮后续已通过 Pencil 打开 `prd/pencil-new.pen` 并核对菜单画板；完整状态验收仍按 QA-07 保留。

### 本轮代码调整

- [x] **UI-01 / P1 / 已实现：统一自绘选择器**。移除灵动岛连接、总览连接、设置连接、统计来源、历史保留时间、导出范围六处可见原生下拉。复用 `SelectField`（Radix 非模态单选菜单，避免 Select 在窗口变尺寸时自动关闭），绑定主题变量、8px 圆角、细边框、浅蓝选中底、勾选标记、禁用态、键盘导航和 Esc 关闭；不引入新依赖。
- [x] **UI-02 / P1 / 已验证：灵动岛菜单裁切与事件隔离**。2026-09-16 在真实 Windows WebView（125% DPR）验证 85/100/115% 自由态收缩与展开、上下左右四边探出/展开及连接菜单边界，全部通过。菜单在窗口内，关闭恢复高度。测试通过 DOM 触发交互并核对真实原生窗口尺寸；物理跨屏拖动仍归 DOCK-03。
- [x] **UI-03 / P2 / 已实现：单一连接按原型只读展示**。总览及灵动岛设置只有一个有效连接且已选中时只显示名称；未选择、多连接或原连接失效时保留选择流程，不静默换号。
- [x] **UI-04 / P1 / 已实现：日期输入取消系统日历弹窗**。改为主题输入框与已有应用日历联动；支持直接输入 `YYYY-MM-DD`，非法日期及反向区间禁止提交，跟随当前时刻时禁用结束输入。
- [x] **UI-05 / P1 / 已实现：连接状态如实着色**。总览不再将过期、无效、离线连接都显示成绿色“已连接”，无连接也不显示绿色成功态。
- [x] **UI-06 / P1 / 已实现：请求日志来源文案**。请求日志始终来自本地记录，不能因组织用量接口成功就标成“官方组织汇总”；桌面加载/失败也不能因 `summaryLive=false` 显示“设计示例”。本地汇总卡同样修正来源与标题，并移除“其他来源已可选”的误导文案。

- [x] **UI-07 / P2 / 已实现：自绘右键和托盘菜单**。2026-09-16 两个入口统一使用按需创建的独立透明 WebView，替换原生 Menu/popup_menu；跟随主题、工作区限位、失焦释放、键盘导航及 Esc 返回/关闭。按 Pencil 菜单原型采用 268px 宽、32px 行高、13px 字号和图标；连接/位置采用同窗返回页，避免横向子菜单越界。真实窗口已验证打开、两层导航、当前连接、退出释放及 125% 下无裁剪。系统文件选择器保留。

### 仍需外部环境或额外范围的项目

| 对应条目 | 当前缺口 |
|---|---|
| QA-02 | 本机 Claude API Key（已配置网关）与 Codex Auth 查询通过；Claude Auth、组织 Admin API 尚缺账号 |
| DOCK-03 | 已按用户确认的单屏范围验收，多屏不纳入本轮 |
| QA-04 | 已通过隔离桌面增删、更换、导入去重、导出和清理流程 |
| QA-05 | 干净 Windows 安装、升级、卸载和 WebView2 依赖验收 |
| QA-06 | 长期运行、多会话及不同 WebView2 版本性能基线 |
| QA-07 | 所有原型画板的浅深色、空/加载/失败等完整视觉验收 |
| OPT-03 | 其他语言及翻译范围尚未指定，现有简体中文继续可用 |
| OPT-05/06/07 | 用户已明确本轮暂不纳入分析页、扩展平台和额外采集来源 |

### 前序验证记录（后续改动以各条目最新记录为准）

- 构建：`pnpm --dir frontend build` 通过（含 TypeScript 检查）。主包约 517.64 kB / gzip 160.99 kB，触发 500 kB 提示；后续拆分纳入 QA-06，不通过提高警告阈值掩盖。
- 浏览器：实际设置页导出范围选择、历史保留菜单通过；共享组件用明确标注的模拟连接测试选择、禁用项、方向键、Esc 和窗口尺寸变化，通过；没有执行真实账号切换、历史清理或导出请求。
- 日期：非法 `2026-02-30` 显示错误且禁止确定，改为 `2026-02-20` 后恢复可提交，通过。
- 视觉：浅/深色菜单已截图并目视检查，8px 圆角与主题底色正确。当前源码无 `<select>` / `input[type=date]` / `input[type=time]`，浏览器可见原生 select 数量为 0。
- 证据：[浅色菜单](output/playwright/connection-menu-light.png)、[深色菜单](output/playwright/connection-menu-dark.png)、[设置页菜单](output/playwright/settings-select.png)。测试夹具已移除，截图仅用于验收，模拟连接没有进入产品数据。
- UI-03 / UI-05 / UI-06 为静态分支与构建验证；真实连接失效、多账号及操作系统缩放仍随 QA-04 进行桌面验收。UI-02 / UI-07 后续已完成；QA-07 完整画板验收仍保留。

## 1. 工程、桌面基础与窗口生命周期

依据：[根目录脚本](package.json)、[Tauri 配置](backend/tauri.conf.json)、[应用入口](backend/src/lib.rs)、[主面板](frontend/src/views/MainPanelWindow.tsx)、[窗口标题栏](frontend/src/components/panel/Chrome.tsx)。

- [x] **BASE-01 根目录启动入口**：支持 `pnpm tauri dev`；根目录管理 Tauri CLI，前端保留独立依赖，开发及构建前置命令指向 `frontend/`。此前已验证 CLI 可识别项目并启动应用。
- [x] **BASE-02 Windows 开发环境**：Tauri CLI、Rust/MSVC、Windows SDK、WebView2 已就绪；此前完成 Rust 编译链接与 Tauri 环境检查。
- [x] **BASE-03 双窗口与基本窗口属性**：灵动岛透明、无系统边框、默认置顶、不占任务栏；主面板使用独立窗口和内部总览/设置导航。配置与入口均已接通。
- [x] **BASE-04 主面板标题栏按钮**：2026-09-16 参照 CC Switch 的 Windows 标题栏交互重做，最小化、最大化/还原、关闭使用连续的 44×40px 点击区；普通按钮为中性悬停，关闭按钮为红色悬停与白色图标，支持键盘焦点和双击标题栏最大化。三项继续调用真实窗口 API，最大化状态变化后自动切换还原图标，失败就近提示；关闭主面板后托盘、灵动岛与采集继续运行。125% DPR 浏览器实测按钮为 44×39px、页面无横向溢出，前端类型检查通过。
- [x] **BASE-05 关闭主面板不退出后台**：后端拦截主面板关闭并隐藏，保留灵动岛与后台采集。这里只勾“不退出”，资源释放见 BASE-07。
- [x] **BASE-06 启动扫描不再阻塞界面主线程**：首次扫描交给 watcher；数据库命令使用阻塞线程池；托盘刷新也已后台化。2026-09-15 已通过 `cargo check`、数据库锁竞争测试，重启后连续检测窗口 `Responding=True`。
- [x] **BASE-07 主面板按需创建与关闭释放**：2026-09-16 从静态窗口配置移除隐藏主面板，由 Rust `WebviewWindowBuilder` 首次打开时创建；关闭采用默认销毁，重开重新挂载订阅和查询，后台采集与灵动岛不受影响。首次创建的页面意图通过后端待消费状态传递，不依赖可能丢失的早期事件。
- [x] **BASE-08 最小化/不可见时暂停非必要工作**：2026-09-16 主面板监听原生最小化/可见状态及页面可见性；不可见时卸载总览/设置内容，停止图表、额度及网络 hooks，恢复后重新挂载并查询；关闭时由 BASE-07 直接销毁 WebView。
- [x] **BASE-09 灵动岛通过后端命令打开主面板**：2026-09-16 统一由 Rust 执行显示、解除最小化和聚焦，不再依赖灵动岛 WebView 的窗口管理权限。
- [x] **BASE-10 打开主面板定位灵动岛当前范围**：2026-09-16 后端随打开事件传递持久化设置，主面板切回总览并定位当前平台与具体连接；后续浏览仍不反写灵动岛。
- [x] **BASE-11 主面板标题栏拖动**：2026-09-16 标题栏空白区域调用后端原生拖动命令，窗口按钮阻止冒泡且保持原操作。
- [x] **BASE-12 主面板事件订阅释放竞态**：2026-09-16 异步订阅增加停止标记，卸载后才返回的监听器会立即释放，不再残留迟到监听。

## 2. 总览真实数据与平台/连接/来源选择

依据：[总览](frontend/src/views/OverviewView.tsx)、[查询 hooks](frontend/src/lib/useUsage.ts)、[设置](frontend/src/views/SettingsView.tsx)、[应用外壳](frontend/src/App.tsx)、[持久化设置](backend/src/settings.rs)。需求 §1、§6.1、§7.3。

- [x] **VIEW-01 桌面端统计不再混入示例数据**：2026-09-16 三类查询均从空/加载态开始，切换范围先清旧数据，失败显示真实错误；设计数据仅保留在浏览器预览。
- [x] **VIEW-02 Auth/API 页面由具体连接决定**：2026-09-15 总览按选中连接的 `kind` 切换订阅额度、网关余额或官方组织 API 用量，不再由桌面端演示 `variant` 决定。
- [x] **VIEW-03 成功余额映射真实响应**：2026-09-15 sub2api 余额金额与币种进入余额卡；官方直连 API Key 不再显示示例余额，改为组织用量或本机统计回退。
- [x] **VIEW-04 移除总览硬编码账号与 Key**：2026-09-15 当前连接、接入类型、更新时间和来源均由真实连接或查询状态生成；未知套餐不再显示固定 20x。
- [x] **VIEW-05 总览平台切换与灵动岛平台分离**：主面板 `viewPlatform` 与持久化 `island_platform` 分开；总览切换平台不会直接改变灵动岛平台。
- [x] **VIEW-06 主面板多连接选择与恢复**：2026-09-15 唯一连接自动采用，多连接要求显式选择并按平台记忆稳定 ID；已保存连接被删除后保持空选，不静默切到剩余账号。
- [x] **VIEW-07 灵动岛保存具体连接和来源**：2026-09-15 `Settings` 增加稳定 `island_connection_id/island_source_id`，选择连接时同步平台与 Auth/API 类型并落盘，重启可恢复。
- [x] **VIEW-08 来源能力与来源选择闭环**：2026-09-16 后端提供稳定来源 ID、所属平台、可用性、能力列表和不可用原因，并校验保存的来源属于当前平台；前端唯一来源自动采用且只读显示，多来源才显示单选，零来源、读取失败和失效来源分别说明，不静默切换。当前实际适配器仍只有各平台本机会话记录。
- [x] **VIEW-09 设置预览反映真实配置**：2026-09-16 指标预览接入当前平台本机今日 Token、估算费用、Auth 窗口、网关余额或官方组织用量，并显示对应查询失败原因、连接成功更新时间及本机采集更新时间；生产设置页移除“四种来源”和固定失效账号设计样例。
- [x] **VIEW-10 无连接与失效连接引导闭环**：2026-09-16 灵动岛区分未选择、已过期和具体失效连接；从灵动岛打开主面板会带入连接 ID，总览提供“添加连接/修复连接”并直接打开连接弹窗。
- [x] **VIEW-11 按账号不可分的数据范围说明**：2026-09-15 灵动岛、设置和总览回退来源均明确标注“本平台本机记录，无法按账号或 Key 区分”。
- [x] **VIEW-12 平台能力配置和更多平台菜单**：2026-09-16 平台可用性与 Auth/API、额度、组织用量、本机会话、实时增量等能力集中到唯一配置源；工具栏“更多”改为可聚焦、可键盘展开的真实菜单，逐项显示可用/未连接/待接入状态，未接入平台保持禁用，新增平台不再只凭图标冒充可用。添加连接入口也按同一能力配置开放分支；前端类型检查通过。
- [x] **VIEW-13 未接入平台不冒充可用**：日常总览默认只提供 Claude/Codex；添加连接中的 Gemini/Grok 标记待接入并禁用。
- [x] **VIEW-14 / P2 / 已对齐：设置页不显示工具栏平台选择器**。2026-09-16 工具栏仅在总览显示平台切换；灵动岛设置从全部 Claude/Codex 连接中选择具体连接，平台随连接同步并只读回显。类型检查和桌面设置页验证通过。

## 3. 连接管理与凭证流程

依据：[连接弹窗](frontend/src/components/settings/ConnectionDialogs.tsx)、[连接表](frontend/src/components/settings/ConnectionTable.tsx)、[连接 hook](frontend/src/lib/useConnections.ts)、[连接存储](backend/src/connections.rs)、[本机凭证适配](backend/src/creds.rs)。需求 §6.3。

- [x] **CONN-01 连接列表与手工 Key 入库基础**：可列出已有连接，手动填写名称和 Key 后经 IPC 保存；返回 DTO 不含 secret，界面使用脱敏标识。此项不代表“验证并添加”已实现。
- [x] **CONN-02 行内获取本机凭证**：对已有连接读取本机凭证、更新脱敏值，失败不清空旧凭证；前端有获取中/成功/未找到状态及读取范围回显。
- [x] **CONN-03 移除确认与保留统计**：确认弹窗调用后端删除连接行，不删除 `requests` 历史记录。
- [x] **CONN-04 使用本机凭证新建连接**：2026-09-15 新建时未提交 secret 会按平台与类型读取本机凭证；读取失败直接返回错误，不留下空凭证连接。
- [x] **CONN-05 添加前验证凭证**：2026-09-16 后端先解析本机凭证、校验地址并调用对应只读接口，验证完成后才用事务外结果入库；失败留在弹窗且不产生半成品连接。
- [x] **CONN-06 / P1 / 已实现：CLI 授权引导**。2026-09-16 启用连接表按钮，弹窗提供对应 CLI 登录命令、复制、取消与“已登录，重新获取”；复用本机凭证读取和账号防覆盖校验，区分失败和更新成功，不把凭证更新当作额度查询成功。真实组件隔离夹具验证成功/失败/取消；桌面实际验证打开/取消。没有重新登录用户真实账号。
- [x] **CONN-07 安全更换 API Key**：2026-09-16 新 Key 先按原平台和网关验证，失败保留旧值并就近提示，成功后原子更新脱敏标识、状态和同步时间并广播连接变更。
- [x] **CONN-08 Auth 多账号名称**：2026-09-16 Auth 添加分支支持填写连接名称，并与生成的稳定连接 ID 一起保存。
- [x] **CONN-09 连接变更跨窗口通知**：2026-09-15 添加、删除和本机凭证更新成功后广播 `connections-changed`，各窗口的连接 hook 自动重查。
- [x] **CONN-10 移除当前灵动岛连接保留身份说明**：2026-09-16 设置同时持久化稳定连接 ID 与显示名；删除后不自动换号，灵动岛显示原连接名和“不可用”。
- [x] **CONN-11 连接错误反馈完整**：2026-09-16 列表区分加载、空、首次失败及保留旧结果的刷新失败；添加、换 Key、移除均就近显示原因并提供重试入口。
- [x] **CONN-12 本机候选可重新发现**：2026-09-16 添加弹窗可随时重新扫描 Claude/Codex CLI 凭证，显示扫描中状态；能力说明与当前四类适配和本机读取范围一致。
- [x] **CONN-13 多账号获取防止静默覆盖**：2026-09-16 同平台同类型存在多个连接时，若本机当前凭证的脱敏身份与目标连接不同，后端拒绝更新，行内明确显示“账号不同，未覆盖”，引导新增连接或切回对应 CLI 账号。

## 4. 额度、余额与费用

依据：[查询适配](backend/src/quota.rs)、[查询命令](backend/src/lib.rs)、[查询 hook](frontend/src/lib/useQuota.ts)、[额度映射](frontend/src/lib/quotaMap.ts)、[余额卡](frontend/src/components/panel/BalanceCard.tsx)、[价格估算](backend/src/pricing.rs)。需求 §2.5、§7.4、§7.5。

- [x] **QUOTA-01 只读请求与错误分类基础**：已有 Claude OAuth/sub2api GET 查询实现，15 秒超时，区分 401、403、429、不支持和普通失败；不将官方 API Key 错当 OAuth 查询。仅确认代码，真实服务联调另见 QA-02。
- [x] **QUOTA-02 可变额度窗口基础**：后端返回窗口数组及可空百分比/重置时间，展示组件可处理缺少分母或倒计时；不要求每个平台一定有 5h/7d。
- [x] **QUOTA-03 Codex 官方额度具名连接链路**：2026-09-15 `connection_quota` 已按所选 Codex Auth 凭证调用官方只读额度接口，主面板与灵动岛共用具体连接；仅凭证与本机 auth 完全一致时附带本机 account ID，不伪造缺失窗口。
- [x] **QUOTA-04 套餐元数据只透传可信来源**：2026-09-16 Codex 使用官方 `plan_type`，sub2api 使用 `planName`；Claude 官方用量响应未提供套餐时保持未知，不再把“官方订阅”连接标签冒充套餐档位。
- [x] **QUOTA-05 自动刷新、共享调度与限流退避**：2026-09-16 额度、余额与组织用量统一每 5 分钟刷新；后端按连接共享进行中请求与结果缓存，成功缓存 5 分钟，限流遵守 Retry-After，网络失败指数退避。手动刷新可跳过成功缓存但不能绕过退避；凭证新增、替换、重读或删除会使旧缓存失效。并发去重及强制刷新遵守限流测试通过，前端生产构建通过。
- [x] **QUOTA-06 切换连接防止旧响应覆盖**：2026-09-16 额度和余额都使用请求序号与卸载失效保护，A 的迟到响应不能写入 B。
- [x] **QUOTA-07 旧值过期状态可见**：2026-09-16 查询失败保留最后成功值、失败原因与成功时间，主面板和灵动岛均明确提示数据可能过期。
- [x] **QUOTA-08 实时额度倒计时**：2026-09-16 每 30 秒重算紧凑倒计时，到零保留额度并显示“等待更新”，同时触发一次额度刷新；已增加格式与到零测试。
- [x] **QUOTA-09 sub2api 缺失字段与时间语义正确**：2026-09-16 缺失 usage 保持未知并显示 `— / 上限`，真实零仍为 0%；订阅 `expires_at` 不再冒充日/周/月重置时间，解析测试覆盖两种情况。
- [x] **QUOTA-10 余额范围未知时如实说明**：2026-09-16 sub2api 未返回账户/组织/Key 归属时显示“范围未由接口说明”，不推断共享或 Key 独立；生产余额卡移除设计稿中的固定示例状态和金额。
- [x] **QUOTA-11 灵动岛今日费用接入**：2026-09-15 API 模式优先显示 Admin Key 官方组织费用；无管理权限或官方费用缺失时显示带 `≈` 的本机 Token 估算，不把估算冒充账单。
- [x] **QUOTA-12 估算费用展示基础**：已有按模型估算、未覆盖 Token 计数、日志“估算”标记和订阅月费区别说明。这里只勾展示及基础链路，价格正确性尚未完成。
- [x] **QUOTA-13 Codex 缓存费用不再重复计入**：2026-09-16 Codex 先从总输入中扣除缓存读写再分别计价，Claude 保持独立输入口径；两类已知样例测试通过。
- [x] **QUOTA-14 价目精确匹配与核验时间**：2026-09-16 按 OpenAI 模型文档、Anthropic 官方价格文档及其 2026-05-27 生效价目重新核验标准 API 价格，代码记录来源与核验日期。模型仅接受明确 ID 或官方日期快照后缀，不再用 `contains` 把未来型号套入旧价；缓存读取直接使用各模型官方价格。Claude 缓存写入因本地日志无法区分 5 分钟/1 小时 TTL 而保持不可估算，未知型号同样返回未知。精确匹配、日期快照、未来变体和缓存 TTL 测试通过。
- [x] **QUOTA-15 极小非零费用显示下限**：2026-09-16 非零且低于四位小数精度的费用显示 `<$0.0001`，不再伪装为零。
- [x] **QUOTA-16 网络查询不占用数据库锁**：2026-09-15 额度、官方 API 组织用量和 sub2api 余额都只在锁内读取凭证，释放锁后发起网络请求，再短暂重入更新同步或失效状态。
- [x] **QUOTA-17 四类官方连接适配与能力隔离**：2026-09-15 Claude Auth、Claude API Key、Codex Auth、Codex API Key 分别路由；Auth 只查订阅额度，API Key 只查组织用量/费用，普通 Key 的 403 回退本机统计，sub2api 继续使用网关额度与余额。解析测试覆盖四类能力矩阵、Anthropic 缓存写入与分币费用、OpenAI 缓存输入不重复计数。

## 5. 本地采集、去重与数据可靠性

依据：[采集器](backend/src/collector.rs)、[数据库](backend/src/db.rs)、[监听器](backend/src/watcher.rs)。需求 §7.5、§7.7。

- [x] **DATA-01 本地来源采集基础**：已发现 Claude/Codex 默认会话目录，支持当前解析器识别的记录结构、字节偏移续读、文件缩短后从头读取。
- [x] **DATA-02 SQLite 存储与基础去重**：有请求、会话标题、扫描偏移、连接表，WAL 及时间/平台索引；请求按 `(source,dedup_key)` 去重。可靠提交和更新语义见下项。
- [x] **DATA-03 扫描结果原子提交**：2026-09-16 每个文件的记录、会话标题、活动状态和扫描检查点在同一 SQLite 事务提交；注入插入失败时偏移和标题均不前进。
- [x] **DATA-04 按真实字节推进完整行**：2026-09-16 增量读取改为字节缓冲，仅消费到最后一个 LF，精确处理 CRLF 并保留未完成尾行；分次写入测试通过。
- [x] **DATA-05 等长重写可重新扫描**：2026-09-16 保存的 mtime 参与扫描判断，文件大小不变但修改时间变化时从头复查并依赖去重约束避免重复。
- [x] **DATA-06 首次扫描和增量扫描的内存/CPU 边界**：2026-09-16 单文件扫描改为逐行读取、每 256 条固定批次写入，不再把全部新增文本和记录集中到内存；记录、标题、生命周期、解析上下文与字节偏移仍在同一事务提交。Codex 的模型、会话和生命周期上下文持久化到扫描检查点，升级后仅一次安全重建，随后不再为新增内容重读全部历史。跨批次及下一轮增量继承模型测试通过。
- [x] **DATA-07 Claude/Codex 实际日志格式覆盖**：2026-09-16 新增显式执行的本机只读冒烟测试，使用临时 SQLite 扫描真实日志且不输出会话标题、提示词或凭证；本机 672 个日志文件无扫描错误，共解析 16,430 条请求，其中 Claude 3,567 条、Codex 12,863 条。另以仅输出字段名的结构审查确认当前 Codex `token_usage_record` 含 response/session/thread/turn 标识及六类 Token 字段，Claude assistant usage 含输入、输出与缓存字段；两平台必须分别解析出非零记录的断言通过。
- [x] **DATA-08 同请求多次更新的求差**：2026-09-16 请求表按 `(source,dedup_key)` 保留各 Token 字段的最终单调值；新增 `usage_events` 记录每次正向差额，实时游标改用事件 ID。相同累计值重复写入不新增事件，10→15 的更新只播放 +5，历史汇总保持单行 15；旧库请求会建立一次基线事件且首帧不播放。回归测试通过。
- [x] **DATA-09 缺失 Token 与真实零区分**：2026-09-16 采集 DTO 的输入、输出、缓存与总 Token 改为可空；数据库为每个字段持久化来源是否提供的标记，累计更新只在拿到真实值时补齐。逐条日志缺失字段返回 `null` 并显示“—”，真实零仍返回并显示 `0`；包含未知总量的周期汇总返回未知，不伪装成零。缺失与真实零回归测试通过。
- [x] **DATA-10 监听失败后保持心跳补采**：2026-09-16 watcher 创建失败或通道断开后不再退出后台线程，继续按 10 秒心跳扫描并记录降级原因。
- [x] **DATA-11 目录后出现与监听恢复**：2026-09-16 每次兜底心跳重新绑定当前存在的 Claude/Codex 来源；晚创建、删除后重建或底层句柄失效的目录会自动恢复事件监听，重绑后立即扫描补漏。
- [x] **DATA-12 事件路径定向扫描**：2026-09-16 notify 回调把变化路径合并进有界集合，去抖后只扫描已知 Claude/Codex 目录内的对应 `.jsonl`；容量 1 的信号合并时路径不会丢失，10 秒心跳继续全量补漏。定向扫描测试确认未触碰未上报文件。
- [x] **DATA-13 采集失败状态传到界面**：2026-09-16 watcher 与手动扫描广播真实错误和最后成功时间；主面板显示原因与重试按钮，灵动岛来源说明显示采集异常，不再把失败伪装为空数据。

## 6. Token 汇总、时间范围、趋势与请求日志

依据：[数据库查询](backend/src/db.rs)、[统计 hooks](frontend/src/lib/useUsage.ts)、[时间选择器](frontend/src/components/panel/DateRangePicker.tsx)、[分页](frontend/src/components/panel/Pagination.tsx)、[日志表](frontend/src/components/panel/RequestLogTable.tsx)、[分项卡](frontend/src/components/panel/UsageHero.tsx)、[面积图](frontend/src/components/panel/UsageAreaChart.tsx)、[图表几何](frontend/src/lib/chartGeometry.ts)。需求 §2.2–2.4、§3、§7.5。

- [x] **STAT-01 自然周期汇总基础**：今日、本周、本月、累计按本地时区查询，周从周一起；四张卡不被自定义范围改成同一区间。时区边界和采集起点修正另列。
- [x] **STAT-02 自定义时间编辑基础**：起止端点、日历翻月、时分输入、跟随当前时刻、起止校验、取消不应用、确定后同步选择范围均有调用链。
- [x] **STAT-03 日志查询与基础分页**：后端按平台和 `[start,end)` 查询、时间倒序、每页 10 条、返回总数；前端页码和上一页/下一页（已知总数）已绑定，切换筛选回到第一页。
- [x] **STAT-04 七列表格与缺失显示基础**：七列结构、横向滚动、模型省略、精确 Token、估算标记、耗时/首字缺失显示 `—`，没有补造 HTTP 200。
- [x] **STAT-05 部分旧请求保护**：汇总/趋势/日志/费用 hook 使用取消标记，旧 effect 的迟到结果不会写入新 effect；额度/余额缺口见 QUOTA-06。
- [x] **STAT-06 分页跳转输入与校验**：2026-09-16 页码输入绑定当前值，按钮和 Enter 均可跳转；只接受 1 到总页数的整数，非法值就近提示并有单元测试。
- [x] **STAT-07 未知总数的降级翻页**：2026-09-16 `totalCount === null` 时不推测页数，上一页/下一页均绑定页码变化；第一页禁用上一页，来源传入 `hasNext=false` 时禁用下一页。当前本地来源仍返回精确总数。
- [x] **STAT-08 加载、失败、重试与刷新状态**：2026-09-16 汇总、趋势和日志均由真实异步状态驱动，失败展示原因与可用重试按钮，切换范围不残留旧平台或演示数据。
- [x] **STAT-09 主面板随新记录更新**：2026-09-16 主面板订阅平台匹配的实时用量和手动刷新事件，重查汇总、趋势及当前日志页，不重置页码。
- [x] **STAT-10 自定义日图结束点正确**：2026-09-16 日分桶使用自定义独占结束时刻的前一毫秒，不再延伸到今天；历史范围测试通过。
- [x] **STAT-11 长范围数据库聚合与点数上限**：2026-09-16 趋势由 SQLite 直接按时间桶求和，跨度超过 180 天时自动扩大日桶且最多返回 180 点并明示粒度；费用先按平台/模型聚合，不再把范围内全部请求载入内存。
- [x] **STAT-12 同一查询结束时间快照**：2026-09-16 每次视图刷新生成一个结束毫秒并同时传给汇总、趋势、费用和日志；翻页保持同一快照，下一次实时/手动刷新才推进。后端回归测试确认快照后的记录不会只进入其中一类结果。
- [x] **STAT-13 采集起点按平台计算**：2026-09-16 累计范围和采集起点都按当前平台过滤，跨平台先后记录测试通过。
- [x] **STAT-14 系统时区、夏令时和跨年日期**：2026-09-16 首期边界明确为操作系统本地时区并在设置页只读显示；周/月起点改用本地日历日期重建午夜，避免跨夏令时按固定 24 小时相减；跨年趋势标签和日志日期带年份。日历周起点保持当地午夜及跨年标签测试通过。
- [x] **STAT-15 / P2 / 已实现：日期范围保护和长范围分桶**。2026-09-16 查询统一拒绝反向区间及超出 1970–9999 年的时间戳；趋势按最长 180 点分桶、日志分页，不任意限制查询跨度。新增 20 年、2 万条记录回归测试，验证点数上限、Token 总量不丢失和日志分页，通过。此结果不代表任意规模性能保证。
- [x] **STAT-16 零 Token 不画假柱**：2026-09-16 零值高度保持 0，只有真实非零值才应用最小可见高度。
- [x] **STAT-17 请求日志状态语义如实展示**：2026-09-16 本地 assistant/token usage 行只证明用量已写入，不提供 HTTP 或任务结果，因此状态统一显示中性“已记录”并使用减号图标，不再伪造“成功”；耗时、首字和 HTTP 码继续留空，只有逐条本地记录进入日志表，汇总接口不生成伪请求。
- [x] **STAT-18 / P2 / 已实现：Token 分项、模型筛选与多系列趋势**。2026-09-18 落地需求 §3 遗留的“输入、输出、缓存读取、缓存写入、请求数、平均每次请求 Token 待确认口径”，参照 cc-switch 使用统计页重做统计区。

  **口径**：新增输入按平台归一——Claude 的 `input_tokens` 本就不含缓存直接用，Codex 的已含 `cached_input_tokens` 必须扣除（见 collector.rs 对 `token_usage_record` 的注释），减法夹到 0。计价仍传**原始** `input_tokens`，Codex 的缓存扣除由 `pricing::estimate` 内部完成（QUOTA-13），两处不得重复减。“真实消耗”直接取 `SUM(total_tokens)`，与四个自然周期同源，不用四项相加，避免页面上下两个数对不上。缓存命中率分母为新增输入 + 缓存创建 + 缓存命中，输出不进分母。

  **未知与零**：四个分项各自保留 `*_known` 标记，缺字段返回 `null` 显示“—”并给出悬停原因，不补零；Codex 会话记录不上报缓存写入，因此该格常态为“—”而非 0。命中率任一分量未知则整体不画进度条——空轨道会被读成“命中率 0”。区间内无记录是真实的零，与未知区分。

  **不做成功率**：`status_code` 只有经本地代理转发才有值，会话记录来源恒为 `NULL`，无法计算真实成功率；该位置改为请求数（`COUNT(*)` 可精确计数），不照搬参照实现的成功率卡。

  **不做双轴**：成本与 Token 量纲不同，叠在同一绘图区时线条交叉点由各自缩放比例决定、不表示任何真实关系，因此拆成上下两格共享一根时间轴，各自纵轴含义明确。

  **筛选与刷新**：模型筛选从 `DISTINCT model` 动态取值（NULL 不进列表），贯穿分项、周期汇总、趋势、费用与日志五类查询，口径一致；换平台或所选模型不在新范围内时自动取消筛选。刷新方式新增“事件驱动（默认）/ 5s / 10s / 30s / 60s”，默认仍由 `live-usage` 文件监听事件驱动，固定间隔只作监听失效时的兜底档，不替换既有事件机制。

  **图表**：不引入图表库（延续 UI-01），手写 SVG 多系列面积图；几何计算全部下沉到 `chartGeometry.ts` 纯函数并单测，未知值断线而不俯冲到 0，面积按连续段分别闭合。图例可点击隐藏系列——缓存命中常比其余三条大一个数量级，全开时那三条压在底边不可读，关闭后纵轴按剩余系列重新缩放；颜色绑定系列身份，隐藏后不重排。

  **配色**：五个系列色经调色板校验脚本（明度带、色度下限、色盲分离度、对比度）确定，非目测。原计划表示缓存命中的 `--purple-text` 与 `--accent-blue` 在正常视力下 ΔE 仅 10.3，低于 15 的可辨下限，改用品红后升至 16.3；其余四色沿用现有语义色。深色一套是独立取值：现有深色语义色为文字可读性挑选，明度 0.70–0.79 全部高于图表安全带 0.48–0.67，直接套用会在深底上发光刺眼且互相难分，故保持同色相与相对明暗关系平移进安全带。绿与橙在红绿色盲下 ΔE 6.2–7.3，处于“需二级编码”区间，由图例文字、分项标签与悬停数值提供颜色之外的身份通道。

  验证：后端 `cargo test --lib` 158 项通过，其中新增 10 项覆盖 Codex 归一化与夹零、缺字段保持未知、Claude 缺 cache_read 不影响新增输入、空区间真实零、命中率分母、模型筛选跨五类查询一致、模型列表跳过 NULL、按桶计价不重复扣减、无价目模型整桶费用作废。前端 `node --test` 50 项通过，其中新增 9 项覆盖刻度取整与等分、未知断线、面积分段闭合、命中索引边界与标签抽稀。类型检查与生产构建通过；浏览器预览浅色与深色两套主题实际渲染核对，图例切换后纵轴重缩放正常，控制台无错误。

  边界：本条只改统计口径与展示，未新增采集字段，也未验证真实 Codex 缓存写入之外的协议差异；“平均每次请求”为真实消耗除以请求数，总量未知或零请求时显示“—”，不做除法。

- [x] **STAT-19 退化空响应不参与统计（2026-09-19 鼠鼠定版）**：已知 Token 字段全为 0 却缺缓存字段的记录——实测为第三方网关空响应（`usage` 只有 `input_tokens`/`output_tokens` 两个 0，无缓存字段）——**不参与任何统计聚合**：分项卡、四张周期卡、灵动岛今日 Token、趋势与费用五处统一排除，请求数亦不计；记录本身保留在库中，请求日志照常显示（`request_log_at` 特意不加排除条件），其总量按「—」显示。实现收敛为 `db.rs` 的 `EXCLUDE_DEGENERATE_SQL` 一处常量，限定 `platform = 'claude'`——Codex 协议本就不上报缓存写入，缺字段在那边是常态而非退化。起因：一条 0-token 网关行触发 `MIN(*_known)=0`，把全天真实消耗、命中率、平均每次与估算费用整体拖成未知；期间排查确认既有“缺失不伪造”口径按设计运行，无计算错误。

  验证：新增回归测试先复现失败（`real_total` 为 `None`）后通过，覆盖分项、周期卡、费用、趋势桶与日志五处断言；后端 `cargo test --lib` 166 项通过；`git diff --check` 无空白错误。真实库只读复查（`dev.ningz.cc-usage/usage.db`）：今日排除后三组 known 标志全为 1，统计请求数 206、合计 32,806,761 与四个分项之和严格吻合，日志 207 条仍含该退化行。前端零改动。

## 7. 灵动岛实时显示与会话

依据：[灵动岛窗口](frontend/src/views/IslandWindow.tsx)、[实时 hook](frontend/src/lib/useLiveUsage.ts)、[窗口组件](frontend/src/components/island/UsageIsland.tsx)、[数字动画](frontend/src/components/island/TokenDelta.tsx)、[会话列表](frontend/src/components/island/SessionList.tsx)、[监听器](backend/src/watcher.rs)。需求 §2.1。

- [x] **LIVE-01 收缩/展开基本交互**：双击切换，收缩态无打开主面板按钮，展开态保留唯一按钮且阻止点击冒泡；真实打开及定位缺口见 BASE-09/10。
- [x] **LIVE-17 会话按首次时间排序并平滑换位（2026-09-19 鼠鼠定版）**：展开态会话列表排序键定为**会话首次出现时间**（该会话请求里的最早时间戳）——最新的会话在最前面；Token 更新只改活跃度、不改首次时间，列表不随增量刷新反复重排，只有新会话出现时才在最前插入。替换原 `reconcileSessionOrder`「既有顺序冻结、新会话追加」的防跳动策略（该策略会让早已失效的会话长期压在最新会话上面，本轮起因即一条 40 多分钟前的失败会话排在运行中会话之前）。后端 `live_usage` 会话查询 `ORDER BY first_seen_ms DESC`（活跃度降序兜底，首次时间取 `MIN(requests.ts)` 相关子查询，残留无请求行排最后）；前端仍直用后端顺序，顺序变化在 `SessionList` 用 FLIP 过渡平滑换位——留存各行 `offsetTop`（列表加 `relative` 作定位父级，内部滚动不产生假位移），对位置变化的行播放约 200ms `MOTION_EASE` 位移动画，首次挂载与系统减少动态效果时直接落位。`sessionOrder.ts` 及其测试随之退役。已同步 PRD §2.1.2。验证：新增排序回归测试先复现失败（活跃度排序把持续出 Token 的老会话排前）后通过；后端 `cargo test --lib` 167 项通过，前端 `node --test` 95 项通过，生产构建含 tsc 通过，`git diff --check` 无空白错误；真实桌面换位动画观感待鼠鼠重启应用后确认。
- [x] **LIVE-16 Codex 真实会话标题与运行状态**。2026-09-15：读取本机 session_index.jsonl，按 ID 关联 thread_name，按更新时间处理改名；移除目录名猜测和短 ID 展示，缺失标题显示“未命名会话”。追加读取 Codex `task_started`、`task_complete`、`turn_aborted`、`final_answer` 生命周期；结束立即移出。实测确认 `compacted` / `context_compacted` 属于任务内自动压缩，不得按日志静默超时移出。已核对当前标题“整理前后端依赖文件目录”和真实数据库状态；标题与生命周期测试通过。
- [x] **LIVE-15 收缩/展开额度排版优化**。2026-09-15：按本轮确认，收缩态隐藏倒计时，展开态用独立 112px 列和禁止换行显示；两行标签、轨道、百分比对齐。展开态合并重复标题、精简平台说明，来源信息分隔展示；状态提示使用完整行宽，设置页预览同步收缩规则。已同步需求文档。验证：类型检查通过；125% 缩放浏览器检查浅/深色截图，倒计时保持单行、额度行无水平溢出，双击展开→收回通过（接口使用测试响应，未改真实数据）。
- [x] **LIVE-14 本机 Codex Auth 额度展示**。2026-09-15：无已选连接时，后端读取本机 Auth 查询固定官方额度地址，凭证不传前端、不落库；启用 Windows 系统代理支持。Auth 收缩/展开/停靠均使用真实额度，缺失项保留“—”，不回退 API 今日费用；按用户本轮确认保留 5h/7d 双行，当前账号未提供的 5h 明确说明。每分钟刷新、支持手动刷新，窗口按实际时长识别，不能把 primary 默认当 5h。验证：真实后端只读联调返回 7d 已用 56% 与重置时间；后端 5 项测试通过，真实账号测试另行显式执行通过；前端额度行 2 项测试、类型检查、浏览器收缩/展开显示检查通过。具名连接与主面板仍见 QUOTA-03。
- [x] **LIVE-02 实时增量连续追数**：2026-09-15：移除会被持续事件推迟的 300ms 防抖；合计和会话明细共用帧循环，以约 900ms 追到已收到的真实 Token。收缩/展开不重播，重复游标不多加，目标达到后停止帧循环，减少动态效果时直接更新。验证：计数器两项测试、类型检查通过；浏览器模拟每 30ms 推送、中途展开及最终 3,000 Token 合计/明细一致通过。日志未提供的用量不做虚构增长。
- [x] **LIVE-03 多会话展示基础**：后端按会话 ID 汇总；Codex 使用真实任务生命周期，其他无可信生命周期的来源保留 90 秒“最近活跃”窗口。前端最多展示三条并提示剩余数量，来源说明包含判定口径。
- [x] **LIVE-04 免打扰基础开关**：设置和托盘共享持久化 dnd，前端停止新增增量提示，后台采集仍工作。完整动效偏好与状态反馈见下项。
- [x] **LIVE-05 订阅后主动获取首帧**：2026-09-16 先注册实时监听，再拉当前平台快照；冷启动或窗口重建不再依赖碰巧到达的首次事件。
- [x] **LIVE-06 实时事件绑定平台**：2026-09-16 事件 DTO 携带平台，切换时先清旧状态并拉新快照，前端丢弃迟到的旧平台事件。
- [x] **LIVE-07 连接失效态与修复入口真实驱动**：2026-09-16 灵动岛按具体连接显示已连接、过期、未选择或“原连接名（不可用）”；打开主面板携带原连接 ID，并提供直达添加连接弹窗的修复入口。
- [x] **LIVE-08 Auth/仅 Token/多窗口布局**：2026-09-16 Auth 灵动岛固定保留 5h/7d 两行，来源只返回周额度时 5h 保持未知长横线，不再把“本地今日 Token”混入订阅额度区；收缩态与展开态规则一致。超过两个额度窗口时灵动岛只收纳前两项并提示剩余数量，完整窗口留在主面板查看。单窗口回归测试与前端类型检查通过。
- [x] **LIVE-09 多会话顺序稳定**：2026-09-16 前端在实时快照间保留仍活跃会话的既有顺序，新会话追加，结束项移除后后续项自然补位；顺序转换测试通过。
- [x] **LIVE-10 完成/失败/来源中断会话状态**：2026-09-16 Codex 的任务开始、正常完成/最终回答和中止事件分别持久化为运行、已完成和失败；完成与失败从会话列表移除，合计 Token 按既有约 2 秒停留 + 300ms 淡出退场。2026-09-16 修复静默误隐藏：取消 Codex 5 分钟过滤，思考、等待输入、压缩期间持续保留本轮会话、起点与已记录 Token；只有来源明确结束事件才结束。采集源异常时现有会话统一显示“状态未知”，其他无生命周期来源继续只称“最近活跃”。生命周期解析、退场查询和上下文压缩回归测试通过，前端生产构建通过。
- [x] **LIVE-11 跨日与历史补采不误触发动画**：2026-09-16 后端只把 30 秒内写入标记为实时增量，历史补采仍计入统计但不播放提示；心跳检测今日值变化，跨午夜即使无新会话也推送新的今日零值，累计数据不清零。
- [x] **LIVE-12 减少动态效果和拖动期间暂停位移**：2026-09-16 保留系统 `prefers-reduced-motion` 直达目标逻辑；原生拖动期间暂停实时计数帧、CSS 动画及位移过渡，松手后从当前目标继续，不清空或重播累计值。
- [x] **LIVE-13 会话更新高亮与本轮已执行时间**：2026-09-16 增量涉及的会话行短暂高亮 800ms（减少动态效果时跳过），每行按本轮任务起点显示已执行时间，新一轮重置，不累计旧会话时长；未知起点显示未知，不编造计时。

## 8. 拖动、贴边与多显示器

依据：[窗口停靠逻辑](backend/src/dock.rs)、[灵动岛交互](frontend/src/views/IslandWindow.tsx)、[停靠条](frontend/src/components/island/DockedIsland.tsx)。需求 §2.1.3。

- [x] **DOCK-01 拖动与四边吸附基础**：Windows 系统移动、24px 判定、工作区计算、四边落位、边缘/偏移保存及启动恢复已有调用链；多屏完整验收见 DOCK-03。
- [x] **DOCK-02 停靠条和 Peek 交互**：120×14 / 14×120 尺寸、空轨道、150ms 探出、500ms 收回、双击解除停靠或展开已实现。2026-09-15 修复停靠来源展开后丢失返回路径：未拖动时第二次双击先回到完整探出态，鼠标移开后自动缩回原边缘；拖动解除停靠后仍回到自由态。状态转换测试与浏览器交互回归通过。
- [x] **DOCK-03 / P1 / 按本轮单屏范围完成验收**。2026-09-16 用户明确只验收当前一个显示器。Windows 125% 缩放下上/下/左/右停靠居中实测通过，稳定中心误差 0.5/0/0.5/0 物理像素；前序三档大小、展开和菜单裁剪通过。多屏拔屏不在本轮范围，不宣称已实测。
- [x] **DOCK-04 吸附提示和收成条的过渡**：2026-09-16 按鼠鼠实机反馈保留灵动岛附近的短 3px 吸附提示条，撤掉横跨整个屏幕边缘的长提示；进入 24px 阈值显示，离开或松手隐藏。横/竖停靠条分别使用 180ms 收拢动画，并遵守系统减少动态效果偏好；Rust 编译检查与前端类型检查通过。
- [x] **DOCK-05 停靠态新增 Token 脉冲**：2026-09-16 每次真实实时增量递增脉冲序号并重启 300ms 水位亮度动画；历史补采、零增量、退场、免打扰和拖动状态均不触发，系统减少动态效果时禁用。判定测试通过。
- [x] **DOCK-06 停靠开关真实生效**：2026-09-16 新增持久化 `dock_enabled`，关闭会解除当前停靠并让悬停/松手不再自动吸附；设置页与后端状态同步。
- [x] **DOCK-07 已停靠状态可直接拖动**。2026-09-15：停靠条和停靠后的 Peek 补齐与自由态相同的 pointerdown 拖动入口；拖离边缘后解除停靠并恢复自由收缩态，拖到边缘仍走原有吸附判定。真实 Windows 窗口验证：修复前 120×14 停靠条拖动前后坐标不变；修复后从 `(691,0,811,14)` 成功拖为自由态 `(527,86,955,243)`。极小工作区与更多 DPI 边界继续由 QA-04 覆盖。
- [x] **DOCK-08 停靠状态同步、尺寸竞态与 DPI 边界修正**。2026-09-15：首轮完成尺寸串行化和物理像素贴边，但用户反馈仍会消失。追加审查在真实 Tauri WebView 中复现：探出后重复同侧停靠，原生窗口只有约 14px 宽，DOM 仍为 428px 宽且有 14px 透明内边距，截图几乎全透明。修复：停靠、解除和重置均发送 `dock-changed`，同侧也重置形态并清除悬停定时器；监听原生 viewport resize，内容未变化但窗口尺寸失配时绕过去重重新同步。验证：新增强制恢复用例先失败、修复后通过；前端 2 项测试、后端 3 项测试及类型检查通过；真实 Tauri WebView 自动注入鼠标进入/离开事件，连续 20 轮探出→收回/同侧再停靠均恢复可见停靠条；直接收窄原生窗口后也自动恢复与 DOM 一致。已核对修复前后真实 WebView 截图。尚不能以有限轮次排除所有长期运行或物理鼠标事件问题，完整桌面验收继续归 QA-04。

## 9. 托盘与常驻交互

依据：[托盘菜单与处理器](backend/src/lib.rs)、[设计预览托盘](frontend/src/components/tray/TrayMenu.tsx)。需求 §2.6；预览组件的效果不等于原生托盘已实现。

- [x] **TRAY-01 原生托盘基础菜单**：已有右键菜单、打开窗口、显示/隐藏灵动岛、平台选择、免打扰、重置位置、关于、退出入口及对应处理器。
- [x] **TRAY-02 托盘平台/免打扰持久化**：平台和 dnd 与设置共享后端状态；平台子菜单区分未连接与未接入，隐藏灵动岛时禁用该子菜单。
- [x] **TRAY-03 托盘入口与最小化恢复逻辑**。2026-09-16：按用户确认，左键单击打开主面板，不切换灵动岛；右键“显示灵动岛”菜单控制灵动岛显示。灵动岛隐藏或最小化时先解除最小化再显示，仅正常可见时隐藏；无边框灵动岛的系统关闭请求只隐藏当前原生窗口，不会在应用退出或开发热重载时误改用户的显示偏好；窗口意外缺失时由托盘按原透明、置顶、停靠配置重建，不再报 `window not found`。右键菜单操作成功后同步持久化设置、前端事件与托盘勾选，“重置窗口位置”也恢复显示状态。实机向停靠态窗口发送关闭请求后确认原句柄仍存在且仅变为隐藏，后端编译通过；完整托盘点击流程仍归 QA-04。
- [x] **TRAY-04 托盘立即刷新真实数据**：2026-09-16 主托盘与灵动岛菜单都会触发本地扫描、额度、余额、组织用量和当前统计重查；刷新期间及 5 秒冷却内菜单禁用并阻止重复触发，失败沿用各查询的旧值过期策略。
- [x] **TRAY-05 原生 tooltip 动态摘要**：2026-09-16 按 Tauri 2 运行时 `TrayIcon::set_tooltip` 更新当前平台、具体连接、本机今日 Token 和更新时间；尚无可共享额度快照时明确显示 `额度 —`。
- [x] **TRAY-06 水位角标**：2026-09-16 后端按当前灵动岛连接全部额度窗口中的最高已用百分比合成 32px RGBA 托盘图标，并同步悬停摘要；低于 75% 为绿、75% 起橙、90% 起红，查询失败或缺失百分比为中性灰。具名 Auth 与本机 Codex Auth 查询均会更新图标；最差窗口选择及“未知不等于 0%”测试通过。
- [x] **TRAY-07 关于信息可交付性**：2026-09-16 版本取 Cargo 构建版本，仓库链接现已切换为 `https://github.com/haishishushu/cc-usage`；关于/设置/总览意图先保存再通知，主面板首次按需创建尚未完成加载时也会主动消费，不再丢失。
- [x] **TRAY-08 托盘摘要：API Key 无套餐改显「今日Token + 剩余金额」（2026-09-19 鼠鼠定版）**：托盘悬停摘要卡在连接为 API Key 且**确认无套餐**（额度来源无 5h/7d 窗口，或明确 `unsupported` 不提供订阅额度）时，不再展示非套餐窗口/提示语，改显两条「今日Token」「剩余金额」；查询失败、凭证失效、限流不算无套餐，仍按原样说明原因。只读现成缓存与本机统计，不穿插网络请求（模块纪律）：今日Token 优先官方组织用量（`API_USAGE_CACHE`，仅 Admin Key 可查），否则回退本机同平台今日（新鲜口径，与灵动岛「本机今日 Token」同源——为此把 live_usage 的今日查询抽成 `db::today_fresh_tokens` 共用）；剩余金额来自 `BALANCE_CACHE`（`12.34 USD` 格式）；任一缺失显示「—」不补零；updated_at 取所读缓存写入时刻的最新值。判定口径与 `dockQuotaView`/`planCoverage` 一致（只有 5h/7d 算套餐）。前端零改动（`TrayTooltip` 本就按 `{key, value}` 对渲染）。验证：新增套餐判定与千分位两组测试；后端 `cargo test --lib` 169 项通过；`git diff --check` 无空白错误。真实托盘悬停显示待鼠鼠重启应用后确认。

## 10. 外观、常规与数据设置

依据：[设置页面](frontend/src/views/SettingsView.tsx)、[通用控件](frontend/src/components/ui/primitives.tsx)、[主题外壳](frontend/src/App.tsx)、[样式变量](frontend/src/index.css)、[字体加载](frontend/index.html)。需求 §4.1、§6.1–6.2。

- [x] **SET-01 双主题样式基础**：有浅色/深色语义变量及浏览器预览切换；此项仅勾样式基础，桌面主题功能见 SET-02。
- [x] **SET-02 桌面浅深主题持久化与跨窗口同步**：2026-09-16 主题写入后端设置并广播到两个窗口，根元素统一切换浅/深变量，重启恢复；本轮已补齐跟随系统，见 OPT-02。
- [x] **SET-03 开机启动配置**：2026-09-16 设置页启动时读取 Windows 当前用户 `Run` 注册项，开关通过参数化 `reg.exe` 调用启用/停用并回读最终状态；不预设默认开启，失败原因显示在本行，子进程使用隐藏窗口标志避免命令行闪烁。Rust 编译检查与前端类型检查通过。
- [x] **SET-04 灵动岛置顶开关**：设置调用 `island_topmost`，后端应用到窗口并保存 `always_on_top`，广播设置变更。
- [x] **SET-05 静默启动与托盘策略**：2026-09-16 更新为可配置“静默启动”，仅开启开机启动时显示，默认开启并持久化；关闭开机启动后隐藏选项但保留选择。静默仅隐藏主面板，灵动岛沿用显示偏好；关闭静默后 Windows 自动启动打开主面板。手动启动保持原行为。托盘仍为固定开启。浅色、深色原型已同步。
- [x] **SET-06 真实数据库路径和占用**：2026-09-16 后端从 `app_data_dir()` 返回实际绝对路径，并合计 `usage.db`、WAL 与 SHM 文件占用；设置页不再显示固定示例。
- [x] **SET-07 打开数据库所在文件夹**：2026-09-16 按 Tauri opener v2 官方 API 从后端打开实际数据目录，目录缺失时创建，失败在设置页就近显示且不引入迁移行为。
- [x] **SET-08 历史保留与清理**：2026-09-16 默认“不自动清理”，可显式选择 30/90/180/365 天并持久化；清理前查询请求、增量事件与已结束会话数量，使用行内二次确认并反馈结果。删除在事务内完成且保留 `scan_state` 偏移，旧日志不会重新补采；选择过保留期后启动时才后台自动清理。预览、删除及偏移保留测试通过。
- [x] **SET-09 导入与导出**：2026-09-16 支持全部、Claude 或 Codex 范围的 UTF-8 JSON 导出，以及系统文件选择后的 JSON 导入；备份只含请求统计和会话标题，明确排除连接、API Key、Auth 凭证与设置。导入校验版本、平台、标识、时间、非负 Token、100 MB/100 万条上限，按来源与去重键合并，取消文件选择不写入，错误和结果就近显示；无凭证泄漏、未知字段语义与重复导入测试通过。
- [x] **SET-10 无行为下拉不再伪装可点**：2026-09-16 当前尚未确定或只有一个来源的选项统一改为带禁用语义的只读值，不显示下拉箭头并说明当前不可配置；已有真实选择使用自绘 SelectField/分段控件，不再使用原生 select。
- [x] **SET-11 离线系统字体方案**：2026-09-16 移除 Google Fonts 外网请求，界面使用 Windows/macOS/Linux 系统 UI 与等宽字体回退链，离线启动不再依赖字体网络。
- [x] **SET-12 键盘操作和弹窗基础交互**：2026-09-16 连接与关于弹窗补 `dialog`/`aria-modal` 语义、初始焦点、Tab 焦点约束、Escape 关闭和关闭后焦点返回；日期“跟随当前时刻”改为真实 checkbox；单选图标均位于可键盘操作的按钮内，会话行可聚焦读取完整标题与更新时间。
- [x] **SET-13 窄窗口、缩放与状态布局**：2026-09-16 在 Chromium 以 820×760、125% DPR 实测浅色与深色主题，页面宽度 805px 时均无页面级横向溢出；连接管理在窄窗口改用紧凑卡片，宽窗口保留完整六列表格。画布状态页实测 Codex 5h 缺失长横线、7d 额度、重置倒计时、过期状态、长会话标题省略与 API 布局；请求日志按既定规则保留表内横向滚动，列与操作可达。前端类型检查通过。
- [x] **SET-14 停靠条「缩小后大小」**：2026-09-19 设置页「大小」下方新增「缩小后大小」（75/100/125%，默认 100%，仅缩放停靠条；探出与展开仍用「大小」）。后端 `island_shrink_scale` 校验、持久化并随 `settings-changed` 跨窗口广播，前端停靠条 shell 按该值 zoom，原生窗口经既有 `resize_island`/`dock::anchor` 通道跟随重新贴边。验证：后端 `cargo test --lib` 165 项通过（含旧 settings.json 缺字段回落 100% 的迁移断言），前端 `node --test` 96 项通过，生产构建含 tsc 通过，`git diff --check` 无空白错误。真实停靠窗口三档缩放的桌面验收未做，归入后续桌面验收。

## 11. 原需求待确认及未来扩展

这些条目不作为“首期必须全部补齐”的依据；已展示的无效控件仍应明确状态。

- [x] **OPT-01 / P3 / 已实现：透明度和大小**。2026-09-16 自绘选择器提供 60–100% 透明度与 85/100/115% 内容大小，后端校验、持久化并跨窗口广播；停靠条保持标准尺寸，探出/展开按所选缩放且测量实际边界。三档真实窗口尺寸与四边菜单验证通过。
- [x] **OPT-02 / P3 / 已实现：跟随系统主题**。2026-09-16 设置支持 light/dark/system，监听系统配色变化并按持久化选择更新；主面板和灵动岛实际 WebView 分别模拟系统深浅变化，均同步通过，显式主题不随系统改变。
- [ ] **OPT-03 / P3 / 部分完成：刷新间隔与语言**。2026-09-16 已实现 1/5/15/30 分钟统一网络刷新配置、后端校验与持久化，额度/余额/组织用量采用相同间隔，平台退避仍有效，本地采集实时运行；桌面修改后读取保存值通过。语言仍为简体中文只读；其他语言及翻译范围未指定，未伪造切换功能。
- [x] **OPT-04 / P3 / 已实现：用户配置余额提醒**。2026-09-16 默认关闭，用户填写金额阈值和三位币种；仅真实、未过期、币种匹配且小于等于阈值的余额在卡内提示，不换汇、不擅设余额等级。后端验证与持久化、纯函数边界、组件保存/关闭/无效输入验证通过。
- [ ] **OPT-05 / P3 / 本轮暂缓：独立分析页、模型排行、Token 明细**。需求 §6.2 将这些列为范围待确认，不因当前缺页就自动扩张首期需求。
- [ ] **OPT-06 / P3 / 部分完成：Gemini/Grok 实际接入**。2026-09-17 Grok 订阅额度已接入（本机 `~/.grok/auth.json` 凭证 + gRPC-Web 计费接口，见「编程套餐额度查询」一节）；Gemini 仍为占位/禁用。Grok 的会话采集、授权中心与 Gemini 的授权/额度/日志来源及口径均未接入，需独立验证。
- [ ] **OPT-07 / P3 / 本轮暂缓：API 汇总/实时请求等额外统计来源**。目前只有本地会话采集，来源名字不代表适配器存在。按平台合法可用接口确定范围；不为补齐字段引入未授权的模型请求代理。

## 12. 验证、交付与文档

依据：[需求性能约束](prd/Pencil-UI设计需求.md)、[后端测试](backend/src/lib.rs)、[前端包配置](frontend/package.json)、[项目说明](README.md)、[前端说明](frontend/README.md)。

- [x] **QA-01 数据库等待不阻塞调用线程的回归测试**：`responsiveness_tests::database_contention_yields_without_blocking_the_caller` 已覆盖锁竞争时让出执行权及后台查询返回；此前执行通过。不能替代采集准确性或 UI 全流程测试。
- [ ] **QA-02 / P1 / 现有账号通过，其他账号缺失**。2026-09-16 按用户提供范围完成 Codex Auth 与本机 Claude API 网关真实只读查询；修复余额型网关无法添加、本机网关地址未带出的问题。401/403/429/500/无效凭证/坏 JSON/缺字段完成本地 HTTP 契约测试。Claude Auth、组织 Admin Key 未提供，完整真实联调保留缺口。
- [x] **QA-03 采集和统计回归样例**：2026-09-16 已覆盖偏移事务、未完成尾行/CRLF、等长重写、流式批次与解析上下文、累计请求更新求差、缺失 Token 与真实零、两平台缓存计费、额度窗口、生命周期、平台起点、统一结束快照和自定义时间边界；追加 672 个本机真实 Claude/Codex 日志的隐私保护只读扫描，两平台均解析出非零记录且无扫描错误。真实日志测试默认忽略，仅在明确进行本机验证时执行，避免常规测试反复读取用户历史。
- [x] **QA-04 / P1 / 隔离核心桌面流程验收通过**。2026-09-16 实际 WebView + 独立数据库完成添加失败/成功、选择、查询余额、更换 Key、缓存刷新、零值、导入去重、导出不含凭证、历史预览/清理、移除保留统计；另连续开关主面板 12 次，每次关闭后仅保留灵动岛页面。脚本和详细边界见 [验收记录](docs/validation/2026-09-16.md)。
- [ ] **QA-05 / P2 / 最新安装包已生成，干净系统待执行**。2026-09-16 最新 NSIS 构建通过，包含会话 Token 和本轮修复；新增卸载 hook 清理实际开机启动键，升级保留。已生成受 Sandbox 用户限制的安装/覆盖升级/卸载/数据保留脚本，PowerShell 语法通过。本机无 Sandbox 或虚拟机，未将脚本通过冒充干净系统执行通过。
- [ ] **QA-06 / P2 / 已建立持续采样，低占用尚未通过**。2026-09-16 隔离 debug 进程树 10 分钟、41 点采样均响应正常，私有内存 246–427 MiB、末值约 381 MiB；连续窗口开关释放页面通过。调试/CDP/HMR 会影响测量，不能直接等同 release。正式版 3 分钟/13 点私有内存 217.7–268.0 MiB，全部响应；补充采样和原始 JSON 见 [验收记录](docs/validation/2026-09-16.md)，长期稳定低占用目标仍未达到可勾选条件。
- [ ] **QA-07 / P2 / 原型全量盘点、状态截图已扩充**。2026-09-16 通过 Pencil 读取全部 48 顶层节点（38 画板、10 组件），按 00–19 类及深色变体列入对照矩阵。完成 12 组区域截图、16 种状态×2 主题及 5 个主面板/日期/设置页面×2 主题，覆盖余额五态/着色、额度阈值、会话数量、日志分页和托盘；临时夹具已移除。完整逐像素及剩余整页状态仍见 [验收矩阵](docs/validation/2026-09-16.md)，不宣称全部视觉验收结束。
- [x] **QA-08 更新过时文档和注释**：2026-09-16 根目录与前端 README 已改用 `prd/` 路径并反映真实采集、连接、查询、拖动和离线字体状态；源码移除“后端未实现/按需创建”等失实说明，剩余联调与验收统一链接到本清单。

## 验收记录模板

完成条目时追加到该条后面：`完成 YYYY-MM-DD；验证：具体命令或操作；结果：通过的行为及边界。`

真实接口不可访问、需求未确认或只有静态组件时，保留 `[ ]` 并写阻塞原因。发现新问题在对应模块新增稳定编号，不删除旧缺陷来提高完成率。


## 会话静默误中断修复（2026-09-16）

- [x] 移除 `db::live_usage` 的 Codex `activity_updated_ms >= now - 300s` 过滤。静默不改生命周期，也不清空本轮 Token；`active_window_seconds = 0` 表示事件驱动。
- [x] 保留明确完成、最终答复、中止的结束判定，以及无开始事件的历史 Token 不得生成运行会话的规则。
- [x] 补充等待 24 小时仍保留 Token / 起点、超过 5 分钟仍运行、思考 / 请求用户输入 / 压缩不会结束或重启本轮的回归。先复现两项失败，修复后 Rust 测试 56 通过、3 项外部账号测试按原设置跳过。
- [x] 隔离桌面验证：24 小时前开始的合成会话保持 `+120 Token`，重复快照不归零；显式 done 后会话移除，合计完成淡出。证据：`output/design-sync/lifecycle-desktop.cjs`、`lifecycle-ended.cjs`、`lifecycle-long-wait.png`。
- [x] 更新灵动岛来源说明，移除“超过 5 分钟无更新隐藏”。
- [x] 本次 `pnpm tauri build` 成功，安装包已更新，正常数据目录下的应用已重新启动；合成验收进程已关闭。
- 范围：本轮修复已确认的 Codex 生命周期路径。Claude 仍属于 90 秒“最近活跃”来源，没有把它冒充有可靠结束信号的“运行中”。


## 历史遗留会话误报运行修复（2026-09-16，覆盖上次仅移除超时的结论）

- 根因：历史日志只有 task_started 而没有结束事件时，单靠持久化 running 无法证明还存在执行者。上次取消超时只修复了等待误退场，却让这些遗留记录重新出现。
- [x] 新增 `session_presence.rs`：Windows 只读探测 Codex `thread-writer-locks/<会话 ID>.lock` 的独占写入锁；不创建、删除、争抢锁。无锁、残留但已释放的锁、无法确认的来源都不能作为运行证据。
- [x] 运行列表同时要求“日志本轮未结束”与“同一个会话的写入者仍存活”；后台推送与前端主动查询共用同一路径，过滤后重新计算运行 Token 合计。仅运行列表降级，不改写生命周期或删除统计历史。
- [x] 长时间思考、等待回答及压缩不依赖日志更新时间；写入者仍持有会话时保留本轮起点和 Token。明确完成后，即使会话仍被打开也不再显示运行。
- [x] 针对性测试：真实 Windows 文件锁的持有 / 释放 / 残留 / 缺失、历史遗留排除、长静默且存活的会话保留、完成但仍被持有的会话排除、合计重算、非法标识路径阻断。Rust 59 项通过、4 项外部环境测试默认跳过。
- [x] 单独执行本机只读验证：4 条日志 running 标记，存活校验后 1 条，排除截图中的 3 条历史遗留记录。未输出会话内容或凭证；保留的 1 条是本轮正在执行的真实任务。
- 能力边界：旧版 Codex 若没有可核验写入锁，或锁无法读取，不会把无法确认的历史状态冒充运行中；Claude 最近活跃口径本轮未变更。
- [x] `pnpm tauri build` 成功；安装包已更新，已使用正常数据目录重新启动修复版。

- [x] **静默启动验收（2026-09-16）**：启动策略 2 项、设置兼容/持久化 3 项测试通过；隔离桌面验证默认开启、条件显隐、关闭后记忆，以及带 --autostart 参数的两种真实进程启动结果。开机启动状态在 UI 验收中模拟，未改用户注册表、未执行系统重启。前端类型检查与 release 构建通过。


## 灵动岛思考/等待/压缩误断 Token 修复（2026-09-16）

- 根因：`session_presence::filter_with` 用锁探测把「锁文件存在但当前未持锁（NotHeld）」与「锁文件不存在（孤儿）」混为一谈，`NotHeld` 直接删除 `activity_state='running'` 的活跃会话。Codex 在思考、等待用户输入、压缩上下文这些静默期会暂时释放字节范围锁，于是活跃会话被误判结束 → `running_tokens=None` → 前端收起 Token。
- [x] 拆分 `Presence` 枚举：新增 `Missing`（锁文件不存在 = 可判定孤儿），与 `NotHeld`（锁文件存在但暂未持锁 = 静默期）区分。
- [x] `filter_with` 只剔除 `Missing`，`Held`/`NotHeld`/`Unknown` 一律保留；会话结束仍由日志生命周期（task_complete/final_answer/turn_aborted）独立裁决，绝不因静默而误杀。
- [x] 更新并补强测试：`orphan_logs_are_excluded_but_silent_owned_turn_is_kept` 改为验证 Held/NotHeld/Unknown 保留、仅 Missing 剔除；`windows_probe_distinguishes_live_lock_stale_file_and_missing_file` 删除文件后断言 `Missing`。
- [x] Rust 测试 63 通过、0 失败、4 项外部环境测试按原设置忽略；`codex_thinking_waiting_and_compacting_do_not_end_or_restart_turn` 通过。
- 权衡：孤儿日志若其锁文件被 Codex 残留（不删除），会重新误报运行；但「误报运行」远轻于「活跃会话被误判结束」，且该场景低频、可在下一次 Codex 明确结束时自愈。锁文件正常删除的孤儿仍会被剔除。


## 编程套餐额度查询：cc-switch 全量对齐（2026-09-17）

- 目标：灵动岛直接显示编程套餐（Coding Plan）的 5 小时/周/月额度，数据源与展示对齐 cc-switch（`coding_plan.rs` / `subscription_grok.rs`，参考副本见 `docs/reference/cc-switch/`）。
- [x] 新增 `backend/src/coding_plan.rs`：按 base_url 域名识别套餐供应商并分发查询——智谱 GLM 中国版 / 国际版（`api/monitor/usage/quota/limit`，裸 key、`unit=3`→5h、`unit=6`→周，credits 用量与套餐档位一并透出）、智谱团队版（`?type=2` + 组织/项目请求头，仅国内站）、Kimi For Coding（`coding/v1/usages`）、MiniMax 中/国际（`coding_plan/remains`，剩余% 反转、周桶仅 status=1 展示）、ZenMux（base 即用量端点，带美元额度）、OpenCode Go（未文档化端点防御式解析）、火山方舟（控制面 OpenAPI AK/SK 火山变体 SigV4 签名，GetAFPUsage→GetCodingPlanUsage 双 plan 探测）。
- [x] 新增 `backend/src/grok_quota.rs`：Grok 积分额度，读本机 `~/.grok/auth.json`（OIDC 优先），gRPC-Web 空 帧 POST 计费接口 + 无 .proto 的 protobuf 启发式解析（最浅 float 百分比、未来 varint 重置时间、零用量周期特判），按重置距今天数映射周/月/credits 窗口。
- [x] 路由接入：`connection_quota` 与 `validate_connection` 对 `api + base_url` 先走套餐识别，未命中维持 sub2api；前端 UI 零适配自动渲染新窗口。
- [x] 辅助凭证独立存储：`plan-query.json`（`settings::PlanQueryStore`），刻意不进 `Settings` 序列化；前端只拿掩码视图（组织/项目 ID 原样、AK 掩码、Secret 仅尾 4 位），设置页新增「套餐查询」分区。
- [x] Grok 平台启用：`platforms.ts` 置 available（仅 subscription_quota 能力），概览页新增本机 Grok 额度卡，`useQuota` 支持本地直查模式。
- [x] 解析层纯函数 + fixture 测试全部移植 cc-switch 踩坑回归（智谱 unit 优先于重置时间排序 #3036、MiniMax 周桶 status=3 不展示、OpenCode 旧扁平形态不识别等）；Rust 120 项通过，真实智谱账号冒烟通过（5h 85% · 10233/12000 credits、周 17%、pro）。
- 范围与遗留：智谱团队版、火山 AK/SK、Kimi、MiniMax、ZenMux、OpenCode Go、Grok 仅完成解析与请求层实现，**真实账号联调待有对应套餐后执行**（冒烟测试模式已就位，参考 `local_zhipu_read_only_smoke`）。Grok token 过期时不做时钟偏差容错（与 cc-switch 的差异，已在模块注释说明）。托盘图标暂不接 Grok。


## 实时 Token 口径对齐终端（2026-09-17）

- 现象：灵动岛「运行中会话 +1.54M Token」与 Claude Code 终端同轮「↓34.0k tokens」相差约 45 倍，且轮次切换时数字会回落。经实库核查（usage.db + 会话 JSONL 重放）确认不是算错，是口径差异：实时增量按 `input+output+缓存写+缓存读` 计，而单轮 13 分钟内缓存重读占 98.4%（每次请求重复计入全部上下文，平均 18.7 万/次）；数字回落则是换轮后 `turn_started_ms` 重置统计窗口所致。
- [x] 新增 `usage_events.delta_fresh` 列（旧行 NULL 回退 delta_total，历史不重算），写入时同步记录新鲜增量（input+output，修正量取 max(0)）。
- [x] `live_usage` 三处切口径：运行中 Token、游标增量会话、灵动岛「本机今日 Token」均改用新鲜 Token；主面板 `token_totals` 保持 total_tokens（含缓存重读）并在概览页加口径说明。
- [x] 回归测试：`running_tokens_count_fresh_input_output_not_cache_rereads` 锁定「运行中/灵动岛今日 = 43、主面板今日 = 3073」的双口径行为；Rust 122 项通过，前端 tsc/vite 通过。
- 遗留：`LiveTokenCounter` 在「服务端快照」与「本地累加」两种模式间切换，换轮瞬间显示值可能跳变（已随口径修正减轻）；如需进一步平滑可在换轮时重置计数器基线。


## API 连接套餐额度显示修复（2026-09-17）

- 现象：智谱（api + base_url）连接的额度后端已可查询，但界面三处仍不显示，反而显示「查询失败（HTTP 404）」——指标预览硬编码仅 auth 连接查额度、灵动岛 `showQuotas` 仅 auth 布局、概览页 api+网关分支只渲染余额卡；余额查询对套餐供应商硬打 sub2api 端点导致 404。
- [x] 后端：`connection_balance` 识别编程套餐供应商后直接返回 Unsupported（「未提供钱包余额接口，请看订阅额度窗口」），不再产生 404 误导。
- [x] 指标预览：所有连接统一查额度，有窗口即显示（百分比 + 重置倒计时）；api 连接额度成功时不再把余额失败当告警。
- [x] 灵动岛：收缩态/展开态改为「auth 或有额度窗口即按额度布局」，展开态标题区分官方订阅/套餐额度，网关余额行保留。
- [x] 概览页：api+网关分支在有额度窗口时渲染套餐额度卡（含套餐档位），余额为 Unsupported 时隐藏余额卡。
- [x] 验证：前端 tsc/vite 通过、24 项前端单测通过；Rust 120 项通过。


## 连接「更新」与本机配置自动同步（2026-09-17，上一会话完成，本次补记）

- 背景：「获取」此前只重读 Key 不更新网关地址；本机配置（如 CC Switch 应用到 `~/.claude/settings.json` 的 env 段）变化后，连接不会跟随，也不主动提醒刷新。
- [x] `connections.rs`：新增 `apply_local` 原地更新连接的 Key、掩码与 base_url（地址规范化比较，无实际变化不报 changed；状态置回 connected 并记录 last_sync_ms）；`fetch_local` 供「更新」按钮复用。防护：本机缺凭证不动连接、同平台同类型另一连接持有不同 Key 时拒绝更新、歧义/暂停连接跳过同步。
- [x] `watcher.rs`：`start_config_sync` 监听本机配置文件，保存后自动同步无歧义连接并发出 `connections-changed` 与 `refresh-requested` 事件，灵动岛即时用新凭证重查。
- [x] 前端：连接表行内「更新」按钮带独立状态（更新中/已更新/已是最新/未找到/不匹配，1.5 秒回落）；`useConnections` 暴露按连接 id 索引的更新状态。
- [x] 回归测试：原地更新刷新、规范化后无变化、缺本地凭证不动、不同 Key 拒绝、无歧义同步、歧义/暂停跳过六项。


## 请求日志「用时 / 首字」与本地代理（2026-09-17）

- 需求：请求日志的「用时 / 首字」要按耗时分档变色（参考 [sub2api](https://github.com/Wei-Shaw/sub2api)），且 **auth 与 apikey 两种连接都要显示**。
- 调研结论：**数据缺失是架构性的，不是解析疏漏。**
  - 本机会话记录里没有请求耗时与首字延迟：Claude 只有 `toolUseResult.durationMs`（工具执行耗时，非 API 请求）；Codex 的 `duration_ms` 挂在 `event_msg/task_complete` 上，是整轮对话耗时，一轮含多次请求，与逐条用量记录对不上。首字延迟两边都不写。
  - sub2api（远程网关）与 [cc-switch](https://github.com/farion1231/cc-switch)（本地 proxy）能有这些数据，**是因为它们都在请求路径上**。本项目只读会话记录，不在路径上。
  - 接网关日志接口这条路已否决：auth 直连官方、网关根本看不到；且实测该网关对普通 key 返回 403。
- 方案：本项目起一个**本地代理**，CLI 的 base_url 指向它，转发到真实上游并计时。auth / apikey 只是上游地址不同，对代理无差别，因此两者都能覆盖。

### 纪律与风险（必须守住）

- **默认关闭的开关**：不开启就保持现在的只读行为，「用时 / 首字」显示「—」。项目现有纪律「全部是 GET 只读，不发送任何提示内容或用量数据到第三方」只在开启代理时才让位，且仅限本机转发。
- **可用性优先于统计**：代理异常时必须能快速回退直连，绝不能让 CLI 不可用。统计是附属品，挡住干活就是本末倒置。
- **两个性能死穴**：① 到上游必须维持长连接池，否则每请求一次 TLS 握手；② 流式响应必须零缓冲透传，否则首字延迟变大、打字机效果卡顿——而首字正是本需求要测的指标。

### 分档配色（对齐 sub2api `frontend/src/utils/latencyHealth.ts`）

四档而非三档，首字与总耗时各一套阈值：

| 档位 | 首字 | 总耗时 | 颜色（浅 / 深） |
|---|---|---|---|
| good | < 10s | < 60s | emerald-600 / emerald-400 |
| warn | ≥ 10s | ≥ 60s | amber-600 / amber-400 |
| slow | ≥ 30s | ≥ 180s | orange-600 / orange-400 |
| critical | ≥ 60s | ≥ 300s | red-600 / red-400 |

### 任务

- [x] 阶段一 · 数据与展示（不依赖代理，先落地）
  - [x] `requests` 表加 `duration_ms` / `first_token_ms` / `status_code`，迁移沿用 `ALTER TABLE ADD COLUMN`；`LogRow` 与日志查询同步返回
  - [x] `latencyHealth.ts`：四档阈值 + `firstTokenSeverity` / `durationSeverity` / `formatLatency`；异常输入（负值 / NaN / Infinity）退回 good，不让脏数据把整列染红
  - [x] `RequestLogTable`：抽出 `LatencyCell`，用时按 duration 档位、首字按 first_token 档位**各自**着色——慢的那项自己变色，才能一眼分清「开口慢」还是「生成久」
  - [x] 状态列接 `status_code`：2xx 成功、429 限流、其余失败；无响应码保持「已记录」，不补造 200
  - [x] `RequestLogRow` 的 `duration`/`ttft` 字符串字段改为 `durationMs`/`firstTokenMs` 毫秒原值，格式化与着色都交给表格
  - [x] 验证：Rust 128 项通过；前端 tsc 与全部单测通过（新增 latencyHealth 7 项）
  - 说明：当前无代理，三列均为 NULL，界面显示「—」——与改动前视觉一致，数据通路和配色已就绪，代理接上即生效
- [x] 阶段二 · 本地代理（2026-09-18 实现；实机冒烟已通过）
  - [x] 依赖：axum 0.8（http1+tokio）+ tokio 显式声明 + reqwest 增加 `stream` + futures-util + toml_edit；hyper / tokio / http 原已是 tauri 传递依赖，运行时复用 `tauri::async_runtime`，不另起 runtime
  - [x] 转发核心（`proxy.rs`）：仅监听 `127.0.0.1`；进程级单例 reqwest 异步 client 长连接池（connect_timeout 15s、pool_idle 90s、tcp_nodelay、**无总超时无读超时**——流式响应间隙不能被掐断）；请求头透传并剥离 hop-by-hop / host / content-length / accept-encoding（交给 reqwest 协商并自动解压），响应剥离 content-encoding / content-length 后 `Body::from_stream` 零缓冲直通；上游 4xx/5xx 原样回传；请求体上限 512 MB（长上下文不被 413）
  - [x] 计时：收到上游第一个字节记 `first_token_ms`，响应流结束记 `duration_ms`，状态码入 `status_code`；从响应流前 64 KiB 防御式提取去重键（Claude `message.id` / Codex `response_id`，都在流开头；提取不到不落库不猜），按 `(source=SOURCE_LOCAL, dedup_key)` 回填 `requests`（`db::apply_proxy_timing`）。记录尚未入库时进 pending 队列（TTL 10 分钟），采集扫描后 `watcher` 在已有数据库锁内 `flush_pending` 补写——CLI 写日志 → notify → 扫描 → flush 时序自然衔接；中途断流不落不完整计时
  - [x] 设置项（默认关闭，不开启时应用行为与纯只读采集完全一致）：代理开关、监听端口（默认 12731）、失败自动回退直连（连续 3 次上游不可达自动还原 CLI 直连配置并停止监听）。开启即自动接管：Claude 改 `~/.claude/settings.json` 的 `env.ANTHROPIC_BASE_URL`、Codex 改 `~/.codex/config.toml` 的 provider `base_url`（toml_edit 保留注释与格式），真实上游存入应用设置；关闭 / 应用退出（`RunEvent::Exit`）还原；启动自愈崩溃残留（`proxy::startup`）；CC Switch 切换网关自动跟随（`on_config_changed`：外部新值存为上游并重写代理地址保持接管，外部切回官方直连则放弃该平台）。**官方订阅直连不接管并如实说明**：Claude 无 base_url / 环境变量上游、Codex 官方订阅（自定义 provider 不使用 auth.json OAuth 凭证，需凭证注入）——CLI 兼容性与凭证注入未验证，不为覆盖面冒进
  - [x] 回归：流式零缓冲端到端（mock 上游分块间隔发送，断言下游首块先于上游整条流发完到达）、429 状态码与 body 原样透传、上游拒绝连接返回明确 502 且不 panic、连续 3 次不可达触发一次回退回调、dedup_key 提取（Claude / Codex 的 SSE 与非流式、取不到不落库）、pending 补写与 TTL 过期、Claude JSON / Codex TOML 保留格式接管还原往返、平台判定（特征头优先 + 路径兜底 + 未知不猜）。「代理崩溃不影响 CLI」在单进程 + `panic=abort` 架构下转化为三个已测等价物：上游异常明确报错不 panic、退出还原、启动自愈
  - 实现期修正：`apply_proxy_timing` 初版误用 `source = 'local'` 字面量，与采集器真实写入的 `SOURCE_LOCAL`（"local-session"）不符会导致静默匹配不到，已改为引用 `crate::collector::SOURCE_LOCAL` 并同步测试口径
  - 验证：Rust 142 项通过（新增 14 项 proxy/proxy_config/db）；前端 tsc + vite 构建通过、39 项单测通过；`git diff --check` 无空白错误
  - [x] 实机冒烟通过（2026-09-18，鼠鼠授权执行）：release 版接管后真实 Claude Code 流量经代理，`requests` 表新增 7 条真实计时（如 ttft 344ms / 用时 100s / status 200，含 pending 补写在真实时序下命中）；`claude -p` 经代理成功返回；关闭后自愈还原直连、`claude -p` 再验证通过、新请求不再计时（三列保持 NULL），CLI 配置稳定为原网关。执行前已备份 CLI 配置至临时目录。过程发现两点：① 从带 `ANTHROPIC_BASE_URL` 环境变量的终端启动应用时，接管探测按设计拒绝（保护逻辑正确）；② 正在运行的 CC Switch 会在代理开启期间以缓存值回写 `~/.claude/settings.json`（一次竞争窗口），应用的 on_config_changed 会跟随并保持接管，最终还原状态正确——后续若开关代理后见异常配置，重启 CC Switch 让它重读即可。
- [x] 采集异常「状态未知」的过激触发修复（2026-09-18，鼠鼠实机截图反馈）：灵动岛曾把 Claude 会话标为「状态未知」而报错的是 Codex 的 rollout 文件（昨晚 Codex 高速写入 12.4 MB 日志、单行 6.1 MB 期间的写入竞争瞬时错误）——跨平台污染 + 单次抖动全局翻车 + 错误文本截断三个缺陷。修复：① `evaluate_collection_status` 连续失败阈值——连续 2 轮（心跳 10 秒/轮，约 20 秒）持续出错才置「采集异常」，单次瞬时错误由下一轮心跳自愈吸收，长期失败计数继续累计供诊断；② `ScanResult`/`CollectionStatus` 新增 `failed_sources`，采集异常按平台定界，前端只有出错平台的会话显示「状态未知」，其他平台照常真实状态；③ 错误文本完整透传（`errors.join("；")`）；④ 新增 `diagnose_app_scan_state_errors` 诊断测试（ignored）：复制应用真实库（含扫描位点）到临时目录用相同增量路径复现采集错误，只输出错误文本。局限如实说明：昨晚错误的具体根因文本已随进程重启丢失（当时未持久化），无法确认是哪一种写入竞争；持续性采集故障（磁盘/锁死）依然会显示「状态未知」——那是正确的诚实信号。验证：Rust 145 项通过（新增 3 项）；前端 tsc/vite 与 39 项单测通过；release 重建（02:57）并替换实机实例，代理关闭态、CLI 直连、upstream 保留均正常。
- [x] CC Switch 回写竞争的常驻自愈（2026-09-18，冒烟发现后增强）：`heal_orphans` 由「仅启动时清理」升级为常驻——配置文件监听回调（`on_config_changed`）无条件先做孤儿检测：CLI 配置指向本机代理端口且该平台未被代理接管时，立即还原为设置里保留的「最近已知网关」（10 秒限流防还原写盘再触发监听的循环）。关闭代理 / 自动回退后**保留** upstream 字段（语义变为最近已知网关，开启时被重新探测覆盖），保证之后的多次回写始终有值可还；还原写入再次触发监听时因地址已不是代理端口而自然终止，无循环。实机验证（release 版）：开启→关闭保留网关 → 手工模拟 CC Switch 回写死端口 → 约 5 秒内自动还原 → `claude -p` 直连正常（"final-ok"）。残余边界：应用未运行期间的回写要等下次启动清理；无已保存上游时只能告警不能凭空还原。验证：Rust 代理相关测试全绿（tray_icon 的图标守护断言失败与本模块无关，系另一 AI 并行重生成图标产物所致，鼠鼠已确认不处理）。
  - 验证补充：`pnpm tauri build` 成功（2026-09-18），release 编译 3m28s 通过，NSIS 安装包已更新为含阶段二代理的 `backend/target/release/bundle/nsis/CC Usage_0.1.0_x64-setup.exe`；未用该安装包启动实机，不冒充冒烟验收

## 主面板 Toast 弹窗反馈体系（2026-09-18，鼠鼠要求参考 cc-switch）

- 需求：主面板的交互要有「弹出后自动消失」的提示，参考 [cc-switch](https://github.com/farion1231/cc-switch)。已与鼠鼠确认三项选择：**全量覆盖并迁移就地提示** / **自研不引 sonner** / **工具栏下方顶部居中**。设计见 [docs/superpowers/plans/2026-09-18-panel-toast-design.md](docs/superpowers/plans/2026-09-18-panel-toast-design.md)。
- 选型依据：cc-switch 用 `sonner`（top-center、成功 2000ms / 失败 4200ms）。本项目已有整套设计变量与 `.motion-*` 过渡，引 sonner 需额外写 CSS 覆盖才能对齐 token，且队列逻辑无法进 `node --test`；自研 150 行即可，零新增依赖。
- [x] `lib/toastQueue.ts`：纯函数队列——同内容不堆叠只重置计时（连点「更新」不刷屏）、上限 3 条且超限者转入退场而非硬删、悬停暂停后按暂停时长整体平移 `createdAt`、`toastRemaining` 对 `duration=0` 返回 Infinity。配 12 项单测。
- [x] `components/ui/Toast.tsx`：`ToastProvider`（api 与队列拆两个 context，调用方不被队列变化牵连重渲染）+ `ToastViewport`（面板根节点内 `absolute top-24 z-[60]`，`aria-live="polite"`，danger 另加 `role="alert"`）+ `useToast()`（无 Provider 时返回空操作，浏览器预览的 Showcase 不会崩）。四档 tone 直接用 `--success-soft` / `--accent-blue-soft` / `--warn-soft` / `--danger-soft`，深浅色自动跟随。
- [x] `index.css` 加 `.motion-toast`：进场 `@starting-style`、退场 `data-state="closed"` 140ms，与 `.motion-dialog` 同节奏，reduced-motion 由既有全局兜底接管。
- [x] 调用点迁移：余额提醒保存 / 关闭（表单校验提示仍就地显示，它指的是「这两个框里填错了」）、连接管理（更新凭证 / 启用 / 断开 / 重新连接的结果，行内红字与按钮结果文案撤除，`FetchState` 收敛为 `idle|loading`、`fetchCreds` 改为返回 `FetchOutcome`）、换 Key 与重命名与移除（对话框关闭后才生效，原本毫无回执）、开机启动切换、数据导入 / 导出 / 清理 / 打开目录、套餐凭证保存、代理启停与回退开关、总览「重试采集」。
- 刻意不弹（结果当场可见，再弹属重复播报）：主题切换、平台与灵动岛显示切换、历史保留天数下拉；`useSettings.save()` 失败统一由顶部 `cfg.error` sticky 红条承担（它带「重新读取」动作），Toast 不重复出声。顺带发现并移除了 `cfg.setRetentionDays(...).catch(...)` 这段死代码——`save()` 从不 reject，只返回布尔。
- 保留就地显示（持续状态，非一次性结果）：连接列表加载失败重试条、代理「最近转发错误」、历史清理确认条、开机启动与数据库信息的**初始读取**失败（自动发生而非用户触发；若改成弹窗，浏览器预览一进设置页就会弹红框，`invoke` 在非 Tauri 下直接抛 `not-tauri`）。
- 实机验证发现并修复的真实缺陷：`ToastProvider` 初版把「退场卸载定时器」放在随 `items` 变化的 effect cleanup 里清除，新提示一入队就会取消上一条已排期的卸载，退场条目**永远留在 DOM**（浏览器实测视口堆积 58 个子元素、高 3861px，把还看得见的那条一路顶到面板中部）。改为按 id 记账、排了就不撤、仅在卸载时统一清理。同时把 `nextId.current++` 与 `Date.now()` 移出 setState updater——updater 必须是纯函数，StrictMode 下会双调用。
- 验证：前端 80 项单测通过（新增 toastQueue 12 项）；`tsc -b --noEmit` 与 `pnpm --dir frontend build` 通过；`git diff --check` 无空白错误。浏览器预览实测（Chrome DevTools）：单条渲染与浅深色配色正确、视口落在 y=177（面板顶 80 + 96）、连点 0.7 秒一次始终只有一条（去重生效）、四种不同提示同时触发后 `childCount=3` 且无残留（上限生效）、Toast 盖在 `z-50` 的添加连接对话框之上（层级生效）。**未做 Tauri 实机验收**：预览模式下 `invoke` 抛 `not-tauri`，成功路径（如「已切换到 xxx」「已清理 N 条记录」）只走通了失败分支，成功文案的实机表现待鼠鼠在桌面端确认。
- 已知取舍（待鼠鼠定夺）：提示压在设置页的「设置分区」导航条与「连接管理」标题行之上 2–5 秒。这是「顶部居中」的必然结果，cc-switch 同样压在其顶栏上；若嫌挡，把视口 `top-24` 下移即可。
- [x] 长连接名溢出修复（2026-09-18，鼠鼠实机截图反馈）：CC Switch 传来的连接名可以很长（`claude 5xclaude 5x… · sk-****77f0`），灵动岛展开态把它折成两行顶开岛体、连接管理表格里它画到了单元格外。
  - 灵动岛 `MetricRow`：值原本完全没有截断——容器加 `gap-3`、label 与 hint 加 `shrink-0`、值容器加 `min-w-0`、值本身加 `truncate` 与 `title`（悬停看全名）。
  - 连接管理表格：名称容器原本用 `inline-flex max-w-full`，**百分比 max-width 在包含块宽度不确定时解析为 `none`**，truncate 因此永不触发；改为 `flex w-full min-w-0 overflow-hidden`，名称与类型两列的 `td` 加 `max-w-0`（非百分比，不依赖包含块解析，列宽仍由 colgroup 决定）。窄窗卡片布局同步给 logo 与状态加 `shrink-0`。
  - 验证：用与源码逐字相同的 DOM 结构在浏览器实测——表格 1847px 宽时文字 490px 装得下不截断（正常）、1100px 宽时截断至 277px 且不溢出单元格、灵动岛 352px 内容区截断至 298px 且不溢出。前端 80 项单测、tsc、vite 构建、`git diff --check` 均通过。未做 Tauri 实机复核，鼠鼠可在桌面端确认省略号是否如期出现。

## 点击交互过渡：导航平滑滚动、滑动底板与性能审查（2026-09-18，鼠鼠反馈"点击不丝滑"）

- 需求：按钮与导航条的点击要有丝滑过渡，参考 [cc-switch](https://github.com/farion1231/cc-switch)。
- **选型结论与参考项相反**：cc-switch 的 Button 只有 `transition-colors`（shadcn cva），全局 CSS 仅 `transition-property: color, opacity` 与一个 popover 动画；它**没有**滑动底板、按下位移与 `@starting-style`，而本项目这些都已具备。照搬会是降级，因此本轮只补本项目自己的缺口。
- [x] 设置分区导航改平滑滚动：`scrollIntoView` 的 `behavior` 由 `instant` 改为 `smooth`（仅用户点击时；托盘/程序化定位仍瞬时到位，那是定位不是表演）；`prefers-reduced-motion` 退回 `instant`。
- [x] 新增 `lib/scrollSpyLock.ts`（6 项单测）：平滑滚动途中滚动推导会连续扫过中间分区、高亮一路闪着追过去。点击时把高亮钉在目标分区，滚动静默 120ms 后解锁，另有 1200ms 硬上限兜底——点击已在顶部的分区时平滑滚动不产生任何 scroll 事件，没有上限就会永久钉死。
- [x] 新增 `lib/useSlidingIndicator.ts`：把原先写死在 `Segmented` 里的滑动底板抽成共用 hook（保留既有两条性能纪律：先集中读布局再写样式、只在选项结构变化时重建 ResizeObserver），支持 `plate` 与 `underline` 两种形态。`Segmented` 改为调用它，行为不变。
- [x] 设置分区导航的选中胶囊改为滑动底板（原先只有 `transition-colors`，蓝块在按钮间瞬移）；平台标签（Claude/Codex）的选中下划线改为滑动（原先 `border-b-2` 硬切）。
- [x] 补齐三处"点了没反馈"：页码格子、日历日期格子、主题卡片加 `.motion-button` 与 hover 态，与项目其余按钮统一。
- [x] **性能审查（trace 实证）**：Chrome trace 抓到 ForcedReflow，`SettingsView` 的滚动推导 `update` 累计 14ms 强制同步重排——它在每一帧先 `style.setProperty` 写 CSS 变量使布局失效、紧接着 `getBoundingClientRect()` 读，且每帧对 7 个分区各做一次 `querySelector`。已改为：分区节点按 id 缓存、集中读完再写、CSS 变量值未变则不写。
- 验证：前端 86 项单测（新增 scrollSpyLock 6 项）、`tsc -b --noEmit`、`vite build`、`git diff --check` 均通过。浏览器逐帧取证：点击「数据」分区时底板 transform 从 x=32 缓动滑至 572（11 个中间位置）、scrollTop 经过 41 个中间值、**整个滚动过程高亮只出现过「数据」一个值**（高亮锁生效，未扫过中间分区）；平台下划线 x 由 0 滑至 88（10 个中间位置）、宽度 85→81 跟随标签宽度过渡；覆盖 `matchMedia` 模拟减少动态效果后，滚动位置数由 43 降为 1（正确退回瞬时）。
- **性能结论如实记录（未解决鼠鼠反馈的"卡顿"）**：强制重排的修复方向正确且有 trace 实证，但**没有证据表明它解决了卡顿**。修复后重跑 trace 因数据量超出工具字符串上限而失败（返回的是旧缓存结果，不可用）；改用受控对比，microbench 差异（0.019ms vs 0.016ms/次）落在噪声内；真实滚动中每帧对比新写法平均快约 2ms、长帧 7→6，但**两者平均帧都在 20ms 以上**。进一步加测基线发现：空转完全不滚动时平均帧已达 17.31ms、只滚动 22.13ms、滚动叠加真实高亮反而 16.97ms——三组不可比，测量噪声主导。与前次评审的结论一致：浏览器样本不代表 WebView2，真实卡顿须在 Tauri 桌面端定位。**待鼠鼠指出具体哪个操作卡，才能继续查。**

## 产品改名：AI Usage Island → CC Usage（2026-09-18，鼠鼠要求）

- 命名映射（共 36 个文件、142 处）：

  | 旧 | 新 | 影响面 |
  | --- | --- | --- |
  | `AI Usage Island` | `CC Usage` | 窗口标题、托盘菜单、关于、预览壳、图标 aria-label、README、设计稿 |
  | `dev.ningz.ai-usage-island` | `dev.ningz.cc-usage` | **应用数据目录与安装包升级识别** |
  | `ai-usage-island`（Cargo / npm 包名） | `cc-usage` | 产物改名为 `cc-usage.exe` |
  | `ai_usage_island_lib` | `cc_usage_lib` | `main.rs` 同步 |
  | `AIUsageIsland` | `CCUsage` | 开机自启注册表值名 |
  | `AI_USAGE_ISLAND_TEST_DIR` | `CC_USAGE_TEST_DIR` | 验收脚本环境变量（validation.rs / start.ps1 / README 三处同步） |
  | `x-ai-usage-island-proxy` | `x-cc-usage-proxy` | 本地代理响应头 |
  | `ai-usage-island-{平台}-{日期}.json` | `cc-usage-…` | 导出与采集临时文件名 |

- **鼠鼠已确认：改 identifier 且不做数据迁移。** 后果如实记录：新版本读 `%APPDATA%\dev.ningz.cc-usage\`（全新空目录），改名前的 9.5MB 历史统计、设置、套餐凭证、连接配置**不再被读取**。原数据完好保留在 `%APPDATA%\dev.ningz.ai-usage-island\`（usage.db / usage.db-wal / usage.db-shm / settings.json / plan-query.json），随时可手动复制到新目录取回。旧安装包不会被覆盖升级，会与新版并存，需手动卸载旧版。
- **刻意保留未改的两处历史兼容内容**：① `backend/windows/hooks.nsh` 新增一行同时删除旧键 `AIUsageIsland`——改名前装过并开了自启的机器会残留该键，不清理就会继续拉起已不存在的旧程序；② `output/` 下的历史运行产物（绝对路径快照，非源码）。源码仓库地址已统一切换为 `https://github.com/haishishushu/cc-usage`。
- 项目文件夹名 `ai-usage-island/` 按鼠鼠选择**保持不变**（它是 git 仓库根，重命名等于移动仓库）。
- `prd/pencil-new.pen` 为明文 JSON，按鼠鼠要求替换 68 处（含 2 处全大写 `AI USAGE ISLAND`），替换后以 `json.load` 验证仍为合法 JSON。
- 验证：后端 `cargo check --lib` 与 `cargo test --lib` 通过（165 项，6 项 ignored）；前端 `tsc -b --noEmit`、86 项单测、`vite build` 通过；`git diff --check` 无空白错误；浏览器实测预览壳与窗口标题栏均显示「CC Usage」，浏览器标签标题为「CC Usage」。
- 未做：Tauri 实机构建与安装包验收（`pnpm tauri build`）。改名涉及产物名与 NSIS 配置，安装/升级/卸载行为须鼠鼠在桌面端实测确认。

## 灵动岛未选择连接时自动启用第一个连接成功的（2026-09-19，鼠鼠指令）

- 根因定位（接上节「看不到两条蓝」）：新版数据目录 `%APPDATA%\dev.ningz.cc-usage\settings.json` 里 `island_connection_id = null`（改名换目录后灵动岛从未选过连接），而主面板的「当前连接」是另一套选择，互不相通——灵动岛一直处于「未选择连接」，`connected=false`，按上一节规则画空轨道。旧目录 `dev.ningz.ai-usage-island` 里反而是选了 `claude-1789561649910` 的，佐证是换目录丢的选择，不是连接不可用。
- 鼠鼠指令：灵动岛没启用（未选择连接）时，默认自动启用最先连接成功的。
- [x] `islandPresentation.ts` 新增 `islandActiveConnection(configuredId, connections, platform)`（2 项单测）：明确选择的连接优先（含已暂停，不被悄悄顶掉；所选被删除照旧「所选连接不可用」，不自动换号）；未选择时回退**当前平台**第一个 `status === "connected"` 的连接，其他平台不顶上、没有连接成功不编造。后端新建连接默认即 `'connected'`（db.rs:118），所以本机发现的新连接可直接命中回退。
- [x] `IslandWindow` 的 `active` 改由它计算，额度/余额查询、状态徽标、连接切换高亮等下游全部自动跟随。
- 边界如实说明：回退只发生在「未选择」时；已选但暂停/失效的连接保持显式选择优先，不被顶掉。回退不落盘——设置文件里 `island_connection_id` 仍为 null，用户一旦在灵动岛连接切换器里手动选择，即恢复完全手动；切换器会把自动回退的连接显示为当前选中（它确实是生效连接）。
- 验证：96 项单测全过（新增 2 项）、`tsc -b --noEmit`、`vite build` 通过。桌面端效果待鼠鼠重启确认：未选连接时灵动岛应自动顶上第一个连接成功的连接；该连接是无套餐 Key 的话，停靠条即两条满格蓝。

## API Key 无套餐时停靠条额度（2026-09-18 鼠鼠报告 → 2026-09-19 定版：两条满格蓝）

- 起因（鼠鼠 2026-09-18 发现，确为实现不一致）：灵动岛的收缩态与展开态按连接类型切换布局，但**停靠条不认 `kind`**，`IslandWindow` 永远给它传 `authQuotaRows(...)`，于是 API Key 无套餐时停靠条上挂着两条 `5h` / `7d` 空轨道。
- 结论改判（鼠鼠 2026-09-19 明确）：「这种无套餐的网关 API Key，灵动岛应该是蓝色的才对，2 条是都 100% 的」——无套餐 Key 的停靠条画**两条 100% 满格蓝色轨道**，表示「不设套餐上限、按量计费」。09-18 记录的「不画轨道（干净空条）」方案**就此作废**。小的 09-18 提过的「满格已表示耗尽应染红」的冲突，用颜色区分化解：满格**红**＝套餐额度耗尽，满格**蓝**＝无套餐上限，蓝色与 API 徽标身份色一致，三者（失败空轨 / 蓝满格 / 红满格）可区分。
- 勘误（诚实记录）：09-18 记录为「已实现并验证」的 `lib/quotaVisibility.ts` / `showsQuotaRows` 改动**不在当前工作树中**（文件不存在、全库无引用），疑似当时改动丢失；本次以现行代码为基线直接实施满格蓝方案，无合并冲突。
- [x] 新增 `lib/dockQuotaView.ts` 纯函数 `dockQuotaView(...)`（8 项单测），把停靠条形态收敛为三态：`windows`（查到 5h/7d 套餐窗口按真实水位，auth/api 一致且永远优先）；`unlimited`（API Key 无套餐）；`unknown`（未连接或已暂停、查询中、还没结果、`failed` / `rate_limited` / `unauthorized` / `forbidden`、Auth 缺数据——一律空轨道，失败绝不画满格）。
- 第二轮（2026-09-19 鼠鼠实测反馈「看不到两条蓝」）：首轮条件收得过窄——网关返回**任何**窗口就按真实水位画，若返回的是总配额/月消费这类非套餐窗口且无百分比，停靠条就是空轨道（鼠鼠截图正是空条）。已放宽为与 `planCoverage` 同口径：**查询有结论但无 5h/7d 窗口即满格蓝，哪怕有其他非套餐窗口**（`isPlanCovered` 参数放宽为只读 key，DTO 与界面类型共用一个判定）；非套餐窗口的真实数据仍在岛卡片与主面板展示。另提醒：改动须重启 `tauri dev` / 重新打包后才可见。
- [x] `DockedIsland` 新增 `unlimited` 属性：两条轨道按 100% 满格绘制，填充固定 `bg-accent-blue`（不走 `quotaBarClass`，避免 100% 命中 ≥90% 红色档），Token 脉冲 `dock-token-pulse` 与刷新柔光照常生效。
- [x] `IslandWindow` 停靠条改传 `dockQuotaView` 判定结果（connected / quotaQueried / 状态 / 窗口四输入）；浏览器预览（`isTauri=false`）仍用 `CLAUDE_QUOTAS` 设计示例不变。
- 边界如实说明：①收缩态 / 展开态岛卡片此情形仍显示「今日 Token / 今日费用」两行，**未**改画满格蓝（鼠鼠说的是「2 条」轨道即停靠条；若卡片也要改，待鼠鼠确认）；②网关返回了非套餐窗口（如总配额、月消费）时按真实水位画，真实数据优先于满格蓝。
- PRD 已同步：§2.1.3 新增「无套餐状态」条目、§3 水位着色补蓝色满格例外。
- 验证：`node --test "src/lib/*.test.mjs"` 94 项全过（新增 8 项）、`tsc -b --noEmit`、`vite build` 通过（dist 已更新）。**桌面端实测待鼠鼠确认**：重启 `tauri dev` 或重新打包后，无套餐 Key 的停靠条应为两条满格蓝；若仍是空轨道，说明该连接的额度查询处于失败/凭证失效态（`unknown`），请把主面板额度区的状态文字发给小的继续查。
