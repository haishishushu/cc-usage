# 设置界面前后端完整交付计划

**目标：** 按已确认的 `prd/pencil-new.pen` 统一设置页完成真实读取、写入、失败反馈、跨窗口同步及数据管理。
**架构：** React 展示 Rust 设置快照；Tauri 命令验证输入并可靠持久化后广播。SQLite 保留连接与历史管理现有边界。
**范围：** 连接管理、灵动岛、常规、外观、数据五个分区；不新增尚未批准的平台、语言或采集来源。
**约束：** 当前工作区继续实现，不切分支、不提交；单智能体；仅隔离配置和合成凭证用于验证。

- [x] 设置存储：先用不可写路径复现内存与磁盘不一致；新增 `Store::try_update`，持锁写临时文件并替换，失败保留旧状态；用户命令返回错误。验证重载、失败、连续并发写入。
- [x] 前端设置：替换吞错的乐观更新，统一保存状态和错误；订阅成功后读取初值，避免初始化覆盖新状态；灵动岛选择与主设置共享一次 hook；可编辑区读取期间禁用。
- [x] 数据流程：导入成功刷新容量；保留期保存期间禁用清理；清理确认绑定预览时的天数；所有失败可见且可重试。
- [x] 完整验收：前端构建与已有逻辑测试、Rust 测试；隔离桌面验证五个分区 IPC、连接断开重连、备份导入导出和清理、设置重启恢复。记录无法替代的真实系统/账号验证边界。

接口保持已有 `api` 方法；`island_topmost` 改为返回设置快照，其他设置命令统一返回 `Result<Settings, String>`。失败时不广播成功事件、不更改 UI 已保存值。
