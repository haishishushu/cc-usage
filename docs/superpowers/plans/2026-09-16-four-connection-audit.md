# 四种连接统一审查与修复

鼠鼠确认：会话／Token／耗时与额度／余额／费用均核查并修复。

- 先核查 Codex 生命周期、当前连接隔离、额度窗口解析，不直接假设 Auth 全部正确。
- Claude 按真实日志生命周期接入共用会话表、起点和本轮 Token 累计；Auth/API 共用平台日志，结束立即退出，静默不超时，旧扫描上下文自动重建。
- 四种连接按能力选择查询：Auth 官方额度；直连 API 组织统计（需要权限）；网关自己的余额和额度。状态与数据绑定当前显式启用连接。
- 修复本地估算停留在挂载时刻、错误响应被解释成零、无效响应被标为已连接等查实问题。
- 合成日志回归、解析异常与零值测试、前端状态及构建、必要隔离桌面验证。不写外部 CLI 配置，不修改 Git 状态。
