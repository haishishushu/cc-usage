# 已审查设置稿实施计划

**Goal:** 实现 Pencil H3xmO/Y76WXH 的连接单一职责、导航层级和原生导入导出。
**Architecture:** React 复用现有组件与唯一 island_connection_id；Rust 负责只读本机已应用配置、连接验证及原生文件选择后的读写。
**Spec:** prd/pencil-new.pen；当前会话已批准的所有设计修订。
**Tech Stack:** React / TypeScript / Tailwind / Radix / Lucide、Tauri 2 / Rust / SQLite。

- [x] 后端：本机配置读取入库按平台、类型、完整凭证、地址去重；新记录 paused；连接验证后可启用；禁止启用未连接项。隔离测试验证去重、暂停保留、同脱敏尾号不同凭证不误合并。
- [x] 前端：移除授权/更换/手动新增入口；显示使用中/启用、连接/断开、移除；使用中与启用 86×32。删除重复灵动岛配置；五个一级标题及导航对齐，二级组为窗口行为和指标预览。
- [x] 原生文件：Rust dialog 插件打开单选 JSON 与另存为；取消返回 None；导入限制大小、校验后事务写入；导出临时写入后替换，成功才返回路径。
- [x] 自动验证：前端构建、Rust 71 项、前端 23 项、隔离桌面状态切换/导航/设置回归及文件读写往返通过。单智能体，不修改 Git 状态、不访问真实凭证测试网络。
- [ ] 原生弹窗的人工交互验收：电脑自动化工具 native pipe 不可用，按工具恢复流程重试仍失败；尚未自动完成系统弹窗的选择、取消与覆盖确认。
